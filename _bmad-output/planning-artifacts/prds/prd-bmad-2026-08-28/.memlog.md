---
topic: BMAD Dashboard CLI — local web dashboard for in-flight BMAD process state
updated: 2026-09-01T13:55
---

- (decision) Stakes: public launch (OSS/product) — full-length PRD, compatibility/versioning/adoption are live concerns
- (decision) Working mode: coaching path — walk PM-thinking sections together, elicit rather than author
- (decision) Core product insight (Jamie): dashboard is stage-aware, not a static file index — what it surfaces depends on current BMAD stage; in-flight work is the organizing principle, with drill-down to minutiae available
- (decision) Coaching entry point: Vision + Features (capability-first); re-entry journey captured inline rather than as spine
- (decision) In-flight is a compound state (sprint-status.yaml + epic/story states); NOT inferable as a single linear stage — bmad-correct-course back-flows to PRD/arch mid-sprint, artifact mixes vary, bmad-build runs with no planning artifacts. Dashboard shows concurrent live strands rather than naming one current stage.
- (decision) Read-only confirmed for v1 (Jamie, agreed+reinforced): agents actively write these files during display; fix-sprint-status repair path proves single-writer corruption already occurs. Read-only excludes writes but not affordances — copy-command, open-in-editor, permalinks stay in scope.
- (decision) Large-spec navigation: wiki-style UI carving docs into pages by heading weight (Jamie). Specs reportedly reach ~24k words.
- (change) Constraint discovered: BMAD docs arrive whole OR pre-sharded (dir + index.md), and filenames are fuzzy (prd.md/bmm-prd.md/product-requirements.md) — document identification must be content-based, not filename-pattern-based
- (assumption) OPEN: should tool's heading-weight carve-up defer to BMAD's own sharding when a doc is already sharded? Unresolved.
- (decision) Audience posture resolved (Jamie): density-first default for fluent practitioner + progressive disclosure via unobtrusive inline hint affordances (circled '?') opening concept modals — one interface, not two modes. Consequence: hint content is a maintained glossary with version-drift upkeep, treated as a first-class feature.
- (change) Drafted PRD sections 1-3 (Problem, Vision + framing decisions, Users); Success Metrics deferred to user input
- (decision) Glossary auto-derivation (Jamie's proposal) accepted in modified form: 3 tiers measured — T1 skill descriptions 49/49 reliable, T2 status vocabularies extractable but defined in YAML comments (fragile), T3 concepts not extractable, must be hand-authored. Mechanism detail -> addendum.md
- (decision) Build-time vs runtime: recommend RUNTIME derivation from target project's own .claude/skills (+ shipped curated baseline for T3), not build-step bake-in — target project carries its own BMAD version and _bmad/custom skills; build-time relocates drift rather than removing it
- (change) Created addendum.md with glossary derivation mechanism; PRD will carry only the capability + version-truth requirement
- (decision) PRIMARY JOB REFRAMED (Jamie): oversight — assurance that no overt error, egregious assumption, oversight or hallucination entered generated artifacts. Secondary job: corpus navigation of past artifacts as exemplars/cautionary tales. Live-threads demoted from purpose to organizing structure.
- (decision) Scope boundary set: tool surfaces explicit risk markers + computable structural risk; does NOT judge correctness or detect hallucination (would require LLM evaluator = different product). Human supplies judgement.
- (decision) Metrics: Stage-1 qualitative colleague feedback by deliberate choice, with explicit gate (colleague reports tool surfaced something they'd have missed). Time-based counter-metric REJECTED by Jamie — for oversight, reading time is the work not friction. Telemetry: none.
- (change) Measured limitation recorded: [ASSUMPTION] in 6/49 skills, [NOTE FOR PM] in 3/49 — marker-based surfacing has blind spots; memlog (script-written) is the more reliable oversight signal
- (decision) Oversight depth: SURFACE ONLY for v1 — no LLM evaluation. Tool highlights markers/overrides/structural risk/changes; human supplies all judgement. Keeps deterministic, testable, consistent with read-only + no-telemetry.
- (decision) Corpus scope: CURRENT PROJECT ONLY — all runs and history within the targeted folder. No cross-project library, no disk scanning. Matches CLI target-a-folder model.
- (decision) Scope: use only what BMAD natively provides; identified gaps become candidate upstream community feedback rather than tool features (Jamie)
- (decision) Landing view (Jamie): 'recent activity' list as primary entry point; selecting an item invokes the UI appropriate to that artifact type (review-*.md -> review UI, addendum.md -> another) = polymorphic artifact viewers
- (change) Correctness constraint raised: filesystem mtime is unreliable for activity ordering (git clone/checkout/cp/rsync/no-op saves all rewrite it). Activity must derive from .memlog.md (append-only, script-written) > frontmatter 'updated' > run-folder date > mtime as last resort.
- (assumption) OPEN: is git history admissible as an activity signal? Most reliable for committed artifacts but not BMAD-native, which Jamie's scope answer arguably excludes.
- (decision) review-{slug}.md / reconcile-{slug}.md clarified to Jamie: produced by this workflow's own Finalize step (Reviewer Gate + input reconciliation); present in every finalized BMAD artifact folder, effectively write-only today
- (change) Drafted C1-C9 capability groups (FR-1..FR-44) + NFR-1..NFR-15 covering read-only integrity, concurrency, version compat, performance, security, portability, accessibility
- (decision) Distribution: npx (Jamie) — runnable without install, no dependency added to target project. FR-45/46. Open risk: assumes Node present; BMAD tooling is Python/uv.
- (decision) Live updates DROPPED for v1 (Jamie agreed): refresh is user-initiated, no filesystem watching. FR-37 revised + FR-47 (staleness must be visible). Avoids hardest bug class — watching a tree agents actively rewrite.
- (decision) Activity ordering confirmed by Jamie: .memlog.md > frontmatter updated > run-folder date > mtime last resort (FR-14/15)
- (change) FINDING: {slug} is context-dependent name not identifier (reviewer / source input / reused subject key). Review files live at workspace root OR reviews/ subfolder. Added FR-49/50.
- (change) FINDING: BMAD records no correlation identity linking a work thread across artifact families — only coincidental naming/date joins. Added FR-48 (inferred relationships must be shown as inferred) + new PRD section 7 'Upstream feedback candidates' per Jamie's community-feedback framing.
- (decision) Jamie confirmed FR-17 (activity filtering), FR-34 (customize.toml deviations surfaced), NFR-15 (WCAG 2.2 AA) — [ASSUMPTION] tags removed
- (decision) NFR-7 downgraded to aspirational (Jamie: nice-to-have, refresh button means not a deal breaker). Consequence drawn: manual refresh shifts the binding perf requirement from cold start to REFRESH latency — added NFR-16, since a session is one start + many refreshes.
- (change) FR-7 REVISED per Jamie, overriding earlier draft: no-BMAD-artifacts case now SERVES a plain page (target path + what was looked for) rather than exiting to terminal. Tradeoff noted: exiting is cheaper, serving is friendlier and keeps the 'you always get a browser' contract.
- (decision) Landing view gains a core-artifacts panel distinct from the recency feed (Jamie: 'a core of certain files that are central should have their own tile/list if present'). FR-52. Rationale recorded: recency and centrality are different axes.
- (decision) Centrality grounded upstream, not chosen: bmad-correct-course declares PRD + epics/stories essential (HALTs without them), architecture/UX/spec optional. FR-53.
- (decision) FR-54 (parent inference, low risk): absent core artifact shown as absent, not omitted — absence is itself oversight signal (e.g. no architecture ever written)
- (change) FINDING: epics/stories have no fixed path; story_location is a per-project field inside sprint-status.yaml. FR-51.
- (decision) FR-54 refined per Jamie: absent core artifacts greyed + low visual attraction, informational not a call to action
- (decision) FR-55 added answering Jamie's question — YES, a bmad-build-only project legitimately has no PRD (build implements any intent without planning artifacts). Absence elevated above neutral ONLY when something on disk implies the artifact should exist. Grounded in readiness-gate's own rule: 'a missing document type is only a finding if stories depend on decisions nothing records.'
- (decision) FR-56 added (principle drawn from Jamie's crying-wolf concern): false alarms are a first-order failure — a dismissed signal is worse than no signal, so risk indication errs toward silence over noise
- (decision) Journeys captured from Jamie in his priority order: UJ-1 stage-completion verification (primary, human-in-the-loop before downstream builds on output), UJ-2 requirements/constraint propagation + version-mismatch detection between spec/PRD and architecture, UJ-3 return-from-absence after git pull
- (change) FINDING (limits UJ-2): BMAD has NO version/hash stamping of source docs — source: and inputs: frontmatter appear once each across all templates vs updated: 6x. Drift handled conversationally at authoring time, nothing checkable written to disk. Tool CANNOT verify propagation; can only report relative recency + declared lineage + side-by-side. FR-57..FR-60 added; new upstream feedback candidate.
- (decision) Archaeologist mode (Jamie's term) DEFERRED to future scope for expediency — recorded in new PRD section 9. Noted it is an experience layer over data v1 already holds (C8), not a data-model change.
- (change) UJ-3 sharpens FR-14: git pull rewrites mtimes wholesale, making mtime actively misleading after a pull rather than merely imprecise — reliability hierarchy is load-bearing
- (decision) Git history RESOLVED: NO — BMAD-native activity signals only (Jamie), consistent with earlier scope answer. FR-62.
- (decision) Mitigation for UJ-3 without git: tool records last-viewed marker per project in its OWN state outside the target project (permitted by NFR-2), enabling exact 'changed since you last looked'. FR-61. Tracks user attention rather than commit boundaries.
- (decision) Reviewer gate: Jamie authorized PARALLEL SUBAGENTS for the finalize gate
- (change) CORRECTION from Jamie: 'version mismatch' in UJ-2 means SOFTWARE/dependency versions (spec says react 18, architecture says react 17), not document-version drift. UJ-2 rewritten as Part A (tech version divergence — serviceable, needs no lineage data) + Part B (requirement propagation — only partly serviceable). Earlier document-drift finding retained as Part B, still valid.
- (decision) FR-63/64/65: extract version-pinned technology mentions across artifacts, group BY TECHNOLOGY with source+location so divergent pins are adjacent. Tool presents, never asserts conflict (divergence may be intentional — architecture superseding stale spec). Extraction conservative; must not imply exhaustive coverage.
- (event) Reviewer gate: rubric walker returned 24 findings (2 critical, 9 high, 8 medium, 5 low) — verdict 'unusually honest reasoning, weak buildability'. Criticals: no v1/MVP boundary across FR-1..65; FR-8 content-based identification is a principle with no testable rule.
- (event) Reviewer gate: adversarial returned 'do not build as written' — 5 load-bearing factual claims false. Parent independently verified ALL FIVE against the v6.11.0 install: confirmed true.
- (change) VERIFIED ERROR 1: memlog entries carry NO timestamp (format '- (type) text'); only frontmatter 'updated' has a time, minute resolution, overwritten each append. FR-14 ranks memlog top for activity ordering — it cannot order across artifacts at all. Breaks C3 landing view precision.
- (change) VERIFIED ERROR 2: memlog.py explicitly does NOT enforce the type vocabulary ('the host skill names the vocabulary; the script does not enforce one'). FR-29's rationale (script-written, not convention-dependent) is wrong. Also only 9/49 skills write a memlog and ALL are planning-side — build/sprint stages produce none, so UJ-1 has no memlog after a build stage.
- (change) VERIFIED ERROR 3: marker counts miscounted as skills when they were files — [ASSUMPTION] = 6 files / 4 skills; [NOTE FOR PM] = 3 files / 1 skill (bmad-prd only). 20 of 49 skills are deprecated stubs, so honest denominator ~29. Wrong figure repeated in section 2.3, FR-31 and section 8.
- (change) VERIFIED ERROR 4: FR-34 targets the wrong file — 35 skill customize.toml files are marked 'DO NOT EDIT -- overwritten on every update'; real overrides live in _bmad/custom/*.toml. As written FR-34 would fire ~35 false positives on first run, violating FR-56.
- (override) VERIFIED ERROR 5: git decision was made on a FALSE PREMISE supplied by the parent. BMAD ships bmad-retrospective/scripts/git_evidence.py, documented 'only MEASURES — it never judges' — same boundary as section 2.3. Jamie's 'not BMAD-native' rationale does not hold; decision must be re-offered on correct facts.
- (decision) GIT RE-DECIDED by Jamie on corrected facts: YES use git, with uncommitted working-tree changes ranking ABOVE commit history — a dirty tree is the signature of in-flight agent work (agents write without committing). FR-14 rewritten as 6-tier hierarchy: dirty tree > commit time > memlog frontmatter (minute) > doc frontmatter (date-only) > run-folder date > mtime. FR-62 reversed.
- (change) Applied verified corrections: marker counts fixed in 3 places (4 skills / 1 skill, ~29 honest denominator); FR-29 rationale qualified; FR-68 added (bmad-build writes no memlog — UJ-1's commonest case has no trail); FR-34 retargeted to _bmad/custom/*.toml (was ~35 fabricated findings); FR-8 given testable 4-level precedence + FR-69 unidentified handling; FR-66/67 memlog-has-no-timestamps + 4-format normalization
- (change) Edge cases verified and specced: FR-70 project-root discovery (walk up), FR-71 same-day rerun folder collision (4/7 patterns constant within a day), FR-72 spec/forge folders carry no date, FR-73 run folder vs sharded doc ambiguous by BMAD's own glob, FR-74 story_location may be absolute/outside tree (/custom/stories tested), FR-75 absent sprint-status.yaml is a normal shape
- (decision) FR-76 added answering the adversarial 'self-neutralizing hedge stack' finding: FR-56's silence preference now has a FLOOR — oversight view always reports what was examined, what could not be interpreted, which signals were unavailable. 'Nothing flagged' must be distinguishable from 'nothing checked'.
- (decision) RELEASE BOUNDARY set by Jamie: v1 = C1..C5 inclusive (55 FRs); C6..C9 deferred (21 FRs). Resolves rubric-walker critical #1.
- (change) Consequence of the cut, recorded in PRD not papered over: C6 IS the primary job (section 2.1), so v1 delivers MANUAL oversight (substrate) not ASSISTED oversight. UJ-2 entirely unserved in v1 (FR-57-60, 63-65 all in C6); UJ-1 partly; UJ-3 substantially. FR-55/56/76 sit in C3 so false-alarm discipline + floor ship in v1.
- (change) Success gate SPLIT because the original gate was unmeetable under the v1 cut: v1 gate is access-based (colleagues inspect output through the tool instead of opening files by hand, and find what they came for); the 'surfaced something they'd have missed' gate moves to the release containing C6.
- (change) Open-questions triage: stale 'git resolved: no' entry corrected to 'resolved: yes' (had not been updated after re-decision). All 4 remaining opens given owner + revisit condition; none blocks downstream phases. Resolved-decisions list added so settled questions are not reopened without cause.
- (event) Reviewer gate complete: 3 reviews on disk (~97KB). Criticals and highs resolved in the PRD; medium/low retained in the review files as an implementation-planning checklist.
- (override) RELEASE BOUNDARY REVISED by Jamie: v1 is now C1..C6 inclusive (64 FRs), superseding the earlier C1..C5 cut. C7-C9 deferred (12 FRs). Primary job (oversight surfacing) is now IN v1.
- (change) Corrected parent arithmetic error: C1..C5 is 50 FRs not 55, and the deferred remainder was 26 not 21 — both figures had been written into the PRD release-boundary section. Superseded by the C1..C6 revision (64 in / 12 out).
- (change) Success gate UN-SPLIT following C6's inclusion: original gate ('colleagues report it surfaced something they'd otherwise have missed') restored as the single v1 gate, now assessable at first release. Access-based measure demoted to supporting signal.
- (change) Journey coverage updated: UJ-1 fully served in v1; UJ-3 served within ordering limits; UJ-2 Part A (tech version divergence) served; UJ-2 Part B (requirement propagation) still limited by absent BMAD lineage data — a constraint no release boundary can lift.
- (change) Mechanical integrity check: FR-1..76 complete, no gaps/duplicates; NFR-1..16 complete; sections 1-10 all present. Fixed stale cross-reference NFR-11 cited section 4 (now User Journeys) for the telemetry decision which lives in section 5 — the drift the adversarial reviewer predicted from section renumbering.
- (change) Addendum editorial pass applied (19 findings: 7 structure, 12 prose). Accepted: frontmatter+parent link, pyramid reordering (recommendation surfaced to lead, table above rationale), Notes column split from Timing, terminology settled on 'hand-authored' to match FR-43, 'fails loudly'->'fails visibly' to match FR-44, ambiguous 'both' disambiguated, subject-verb adjacency, 'versus' spelled out, unhyphenated noun 'build time', slash-as-or removed.
- (decision) Editorial rejections with reason: (1) serial comma NOT adopted — prd.md omits it consistently, established house convention, lens itself flagged low confidence; (2) H2 container heading PRESERVED — addendum is the general overflow document for this PRD and further downstream-depth topics are expected to accrue as sibling H2s.
- (change) Addendum QUESTION resolved: org-skill-description exposure consequence now mirrored into PRD section 9 alongside C9's deferral, since it was previously visible only to addendum readers
- (change) PRD editorial pass applied (34 findings: 14 structure, 20 prose). CRITICAL FIX: UJ-3 still read 'Git history is deliberately not consulted' — stale text surviving the re-decision, contradicting FR-14/FR-62/section 10 and pointing a cold reader the wrong way.
- (change) Structure applied: At-a-glance block + v1 table promoted to top (conclusion was ~2400 words in); duplicate marker/memlog statistics deduped to single citations; UJ-2, section 8 and section 9 condensed to gap+pointer form; bold sub-labels added to the three densest capability groups since FR IDs are non-monotonic; release table emoji replaced with Yes/Deferred (an NFR-15 conformance issue in the PRD's own table); working-title blockquote cut from the highest-value position.
- (change) Prose applied: sentence-case headings; product named consistently 'the tool'; serial comma adopted per declared Microsoft style guide (reverses my earlier addendum rejection — a single conformance decision across both documents beats per-document house convention); register drift removed (self-grading asides that addressed a past collaborator rather than a cold reader); Jamie named in section 3 where the primary posture is introduced.
- (decision) Editorial rejections: (1) Oxford spelling (judgement/colour) RETAINED — the lens flagged it as a locale decision, not errors; Americanizing the author's spelling is his call, not the polish pass's. (2) Counter-metric NOT moved out of section 5 — it is a metrics decision and reads correctly in context.
- (event) PRD finalized
- (decision) PRODUCT NAME resolved by Jamie: bmad-dash. Applied to prd.md and addendum.md titles; open question closed and moved to the resolved list. npm package name follows from it per FR-45.
- (decision) Sharding precedence RESOLVED by Jamie: defer to BMAD's existing shard boundaries; heading-weight carving applies only to whole documents. FR-77. Rationale added: keeps tool pages, BMAD files, permalinks (FR-27) and open-in-editor targets (FR-24) referring to the same units. Revisit if page sizes prove unreadable.
- (decision) Node availability RESOLVED by Jamie: not a barrier — BMAD is itself normally installed via npx, so Node is already present. Closes the adversarial finding that called npx 'a launch blocker filed as a note'.
- (override) FR-61 WITHDRAWN by Jamie: tool-side last-viewed marker removed — out-of-project state management is a bug vector (first-run, corruption, project-move, name-collision cases). Activity ranking is best-effort under FR-14 with no memory of previous visits. ID retained, not reused. Also closes edge-case finding M-1. UJ-3 updated to stop leaning on it.
- (decision) Security hardened at Jamie's request: NFR-9 now specifies binding explicitly to literal 127.0.0.1 (not the name 'localhost', which can resolve elsewhere, and never 0.0.0.0), with the honest limit stated — other local users/processes on a shared machine can still reach a loopback port.
- (decision) NFR-17 added (promoting edge-case finding M-9 out of the residual list, given Jamie's security focus): run-folder names are built from unsanitized free-text slugs that reach the tool as both filesystem paths and URL components; treated as untrusted input on both surfaces.
- (change) Counts reconciled after FR-77 added and FR-61 withdrawn: 77 IDs assigned, 76 active; v1 = C1..C6 = 64 active; 12 deferred; 1 withdrawn
- (decision) SUCCESS GATE OPERATIONALIZED by Jamie, closing the adversarial reviewer's sharpest surviving finding: v1 succeeds when aggregate time+mistake costs saved across the user pool outweigh the cost of proceeding to v2. A comparison assessed when the v2 question is asked, and falsifiable — if savings don't clear build cost, v1 did not succeed.
- (override) UJ-2 DOWNGRADED by Jamie: it is a potential MANUAL use case by the operator, not functionality that must be written. FR-63/64/65 (technology version divergence extraction) WITHDRAWN, IDs retained. UJ-2 rewritten as a manual comparison served by C2/C4/C5 rather than by detection.
- (decision) Future direction recorded (Jamie): invoke Claude via its CLI to run consistency checks between USER-NOMINATED files. Nomination keeps scope user-controlled, avoiding the false-alarm cost of scanning everything, and places judgement where section 2.3 says it belongs. Out of scope for v1; this is the concrete form the LLM-boundary re-examination will take.
- (override) FR-57/58/59 WITHDRAWN by Jamie (with 63/64/65): no cross-artifact consistency inference at all. FR-60 RETAINED but reshaped — the USER nominates the two artifacts to compare, so the tool asserts no relationship it cannot evidence. Same nomination model as the section 9 Claude-CLI direction.
- (override) FR-28 (full-text search) WITHDRAWN by Jamie during architecture — out of scope. It was the only requirement forcing a full content read of every artifact, so its removal lets the read layer stay index-eager/content-lazy. Moved to section 9 future scope with a note that it needs its own indexing design. ID retired.
- (change) STRUCTURAL FIX found by two independent architecture reviewers: FR-37 (user-initiated refresh) and FR-47 (staleness visible) sat in DEFERRED C7 while v1's entire refresh model depends on them. Cause: FR-37 began as 'reflect changes while open' — a live-threads concern — and was rewritten into user-initiated refresh without being moved. Both relocated to C3 under a Refresh lifecycle sub-label.
- (change) Amended from the UX run: section 3 now states explicitly that the secondary 'newer user' posture is NOT served in v1, because the concept-explanation layer is C9 (deferred). Section 2.2's density-plus-disclosure framing gained the release consequence. Previously the PRD implied v1 covered both postures.
- (override) FR-7 and FR-70 rewritten during sprint planning (Jamie): resolution looks only at the target path for _bmad and _bmad-output, never walking the tree, so nested/multiple roots become unresolvable rather than resolved by rule. On no project the tool does NOT serve — it exits with what it looked for plus the exact invocations that would work, modelled on git push reporting a missing upstream. A bounded scan (full ancestor chain, two levels down, skipping dotdirs and node_modules) builds only that message.
- (note) FR-7 has now been round a full circle: originally drafted as exit-to-terminal, overridden by Jamie to serve a page, and now returned to terminal — but with actionable suggestions, which is better than either prior position. Recorded so it is not re-litigated.
- (change) Sprint-planning gate follow-through: symlink handling (resolve before confinement check and before keying identity, cycle detection, dangling links) added to Story 1.4; implausible created/updated pairs resolved by taking the newer value as best-guess evidence (Story 3.2); unclassifiable development_status keys treated as legal and rendered present-but-uninterpreted (Story 2.8); Story 1.2 split into token layer (1.2) and shared components plus chrome (1.3), Epic 1 renumbered to 12 stories.
- (change) Process defect found during Story 1.1 loop 2: four spec task-line amendments silently no-op'd because loop 1 had already converted '- [ ]' to '- [x]', so the loop-2 replacement needles never matched. The code was still correct — the dispatch message carried the findings in prose — but the spec, the declared sole source of truth, lost the entire loop-2 requirement set. Tasks section rewritten to match reality and each edit verified individually rather than assumed.
