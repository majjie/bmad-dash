---
topic: bmad-dash UX — read-only local dashboard over a BMAD project's artifacts, for human oversight of machine-generated work
updated: 2026-09-01T17:42
---

- (decision) Sources inherited by reference (not duplicated): SPEC.md (6 capabilities), prd.md (FR/NFR detail incl NFR-14 keyboard, NFR-15 WCAG 2.2 AA), ARCHITECTURE-SPINE.md (AD-2 rendering boundary). addendum.md excluded — it serves deferred C9.
- (decision) Stakes carried from PRD: public OSS launch, WCAG 2.2 AA, keyboard-first (uncontested)
- (decision) Jamie's direction: dashboard-dense appearance; Material Design language; discrete information items each in their own tile, chosen for extensibility; dark theme for now
- (note) Answered Jamie's WCAG question: dark theming does not contradict WCAG 2.2 AA — the criteria govern contrast ratios, not polarity
- (event) Rendered 5 dark palette variations to .working/color-themes-1.html for Jamie to pick: 01 graphite/cyan-slate, 02 ink/amber-signal, 03 near-mono/single-accent, 04 deep indigo/M3-stock, 05 warm charcoal/muted sage. All use M3 tonal elevation; all include the four signal states and a focused control so the focus ring can be judged.
- (note) M3 dark facts verified on the web: elevation is expressed through surface-container tonal tints rather than shadow (shadows are invisible on dark), which suits Jamie's tile requirement directly. Traps recorded: not pure black (halation), accents need desaturating on dark, and WCAG 2.2 adds 2.4.11 focus-not-obscured + 2.4.13 focus appearance which bite in a dense tile grid.
- (note) Tooling limitation: Excalidraw and Figma MCP connectors are unauthorized in this session, so wireframes will be HTML rather than Excalidraw files
- (decision) PALETTE CHOSEN by Jamie: variation 01 Graphite / Cyan-slate. surface #121617, container-low #181d1e, container-high #212829, primary #6fd3c6, on-surface #e5eaea, outline #394345. Register: cool, instrumental, reads like a measuring tool.
- (decision) MATERIAL AS A TOKEN SYSTEM, not a component library (Jamie: keep dependencies low). Adopt M3 colour roles, tonal elevation ladder, shape and spacing scales; build components in Preact. Consistent with the spine's dependency-minimalism constraint and NFR-7. Foundation therefore names M3 as a token reference, not an inherited UI system whose components we defer to.
- (constraint) Tonal elevation is the tile separation mechanism: tiles sit on container-low or container-high against the surface ground, never on box-shadow, because shadow does not read on a dark ground
- (decision) STATIC TILES (Jamie): fixed layout, no drag/resize/dismiss. Rationale — rearrangeable tiles require persisting locations, which AD-12 forbids and which withdrew FR-61. Revisit only if user feedback demands it. Tiles are for DEVELOPER extensibility: a new capability adds a tile without redesigning the page.
- (decision) Form factor: desktop browser only. Narrow-window support is not a target, so the responsive section states a minimum viable width rather than specifying a mobile layout.
- (decision) Working mode: FAST PATH — draft both spines with [ASSUMPTION] tags for Jamie to correct
- (assumption) Voice unanswered by Jamie, inferred: terse and technical, matching the fluent-practitioner default, with explanation carried by the opt-in '?' affordance rather than by inflating default microcopy
- (change) Both spines drafted. DESIGN.md: M3 token frontmatter (surface-container ladder, 4 closed signal roles, 7 type roles across IBM Plex Sans/Mono/Serif, 4px spacing scale, 8 component token sets) + 8 canonical body sections. EXPERIENCE.md: Foundation, IA (6 surfaces), Voice and Tone with a load-bearing string table, Component Patterns, State Patterns (5 per surface), invented 'Evidence and Honesty Patterns', Interaction Primitives with the keyboard map, Accessibility Floor, Responsive, 3 key flows mirroring UJ-1..3, Inspiration and Anti-patterns.
- (decision) Invented section 'Evidence and Honesty Patterns' — product-specific: the tool exists to be trusted about a machine's output, so it must be trustworthy about its own knowledge. Covers evidence-always-shown, unorderable-as-rendered-state, coverage always visible, bounded silence, no unevidenced relationships.
- (decision) IA closure verified: all 6 capabilities land on a surface; every surface is reached by a journey EXCEPT 'No project', which is state-driven (reached by error, not intent) — recorded rather than papered over.
- (question) CONFLICT SURFACED while drafting: the '?' concept-explanation affordance Jamie described in the PRD is capability C9, which is DEFERRED from v1. So the density-plus-progressive-disclosure resolution is not realizable in v1 — the Voice and Tone strings carry the whole explanatory burden alone. Either strings explain more than terse copy normally would, or a minimal glossary is pulled forward from C9. Logged as the spines' only open question.
- (decision) C9 RESOLVED by Jamie: v1 assumes a knowledgeable user base; the '?' concept-explanation affordance lands in v2. Consequence recorded in the spine — the Voice and Tone strings are the ONLY explanation a v1 reader gets, making them load-bearing rather than stylistic. Voice [ASSUMPTION] tag removed.
- (change) Upstream implication to offer to Jamie: PRD section 3 describes a secondary 'newer user' posture served by progressive disclosure. With C9 in v2, that posture is unserved in v1 — the PRD should say so rather than implying v1 covers both.
- (event) Upstream PRD amended (section 2.2 + section 3) so it no longer implies v1 serves the newer-user posture; source and UX contract now agree
- (decision) finalize_reviewers persisted to _bmad/custom/bmad-ux.toml (3 lenses: computed-contrast verification, state-by-surface coverage matrix, cross-spine seam check). Written generically rather than hardcoded to bmad-dash so they apply to future UX runs. Verified: resolves to 3 entries with all other workflow keys intact.
- (event) Reviewer gate: contrast lens computed 88 pairings — 59 pass, 9 genuine AA failures. Parent independently recomputed all headline figures with its own script: every one confirmed exactly.
- (change) CONTRAST FIXES applied and verified: on-surface-faint #6E7C7E->#8B9899 (was 4.20-3.12:1, now 6.11-4.54:1) and its stacked opacity forbidden; outline #394345->#687A7D (was 1.79-1.47:1 as the ghost button's only edge, now 4.05-3.01:1); focus-ring was byte-identical to primary (1.00:1) so a 1px inner stroke in on-primary (8.04:1) added for filled elements; all four signal container fills were 1.00-1.22:1 against level-2 tiles so pills now carry a 1px stroke in their own signal colour (5.88-9.08:1).
- (change) SELF-INFLICTED COLLISIONS fixed: signal-present-container was byte-identical to primary-container, and signal-present cyan sat 1.08:1 from primary — so 'state never borrows the accent' was false in the document asserting it. signal-present is now green #9BD98F (8.19:1 min) with container #243A22.
- (change) FALSE RATIONALE corrected: DESIGN.md claimed unchecked was violet 'so it cannot be misread as a mild unreadable'. Under protanopia present/absent/unchecked converge to one hue, inverting that reasoning. Replaced with an honest colour-vision limit — colour is always paired with the state word, so text is load-bearing rather than redundant. Added a Conformance record section dated and stating that colour changes require recomputation, not re-reasoning.
- (event) Reviewer gate: state-coverage lens found 20% fill rate (6/30 named cells) — the section opened by claiming 'every surface specifies these five' while being five cross-cutting principles written in Dashboard vocabulary. Dashboard 5/5, Artifact view 1/5, all other surfaces 0/5. Verdict accepted in full.
- (change) State Patterns rebuilt as a real 6-surface x 5-state matrix with per-cell specifications plus cross-cutting rules. Added the refresh-failure branch that was entirely missing: a partial scan is DISCARDED not published, since a fresh-looking dashboard with silent holes is the worst outcome available to this product.
- (change) Project header promoted from Dashboard tile to GLOBAL CHROME — it hosts refresh progress and snapshot currency, so two of five states were being specified on four surfaces via a component those surfaces were not specified to have. Overlay pattern named: the artifact nominator, the only modal in v1, previously belonging to no surface in the IA.
- (change) New section 'Conditions beyond the five' covering 9 real conditions the spine had left to developer invention: no git repository (with AD-6's tiebreak made concrete — run-folder date then path), artifact vanished between scan and open, sharded documents (the word appeared zero times before), story_location out of tree, unidentified artifact, dead section permalink, multi-run folder, valid project with zero artifacts, absent core artifact.
- (decision) story_location-out-of-tree resolved WITHOUT breaking the closed signal set: reported at artifact-family level as a configuration property, not as a fifth signal state. The reviewer correctly identified that collapsing it into 'Not checked' would break FR-76's core distinction while inventing a fifth colour would break DESIGN.md's closed set.
- (event) Reviewer gate: cross-spine seam lens found 47 leaks, 4 blocking. All 84 {path} refs resolved and the components block was hex-literal clean, so the mechanical hygiene held; the failures were semantic.
- (change) BLOCKING SEAM FIXES: (1) the one-accent-per-tile rule forbade its own correct implementation — every activity row carries a primary evidence badge by requirement; rule rescoped to controls and headings, with repeating data columns exempt because an accent used as encoding is not emphasis. (2) 'present' had no word despite the spine promising all four states in words — added. (3) signal-pill had no colour token, which is why all four signal-*-container values sat orphaned; it now maps colour/background/stroke per state. (4) refresh progress had no visual spec and DESIGN.md had no motion tokens at all, despite being the only animation in v1 — added motion tokens, a Motion section, a refresh-progress component, and prefers-reduced-motion handling.
- (change) Type floor corrected: the rule forbade type below body-dense 13px while four of the document's own roles were smaller. Split into two floors — content text 13px, labels and badges 11px — and mono-badge raised from 10px to 11px because its text is the redundancy that keeps signal state legible without colour, so it must not be the smallest thing on screen.
- (change) IA CLOSURE CLAIM CORRECTED — it was short by two. The spine claimed only 'No project' lacked a journey; Oversight and Document reader had none either. Fixed honestly on both sides: the claim now says five of six, and UJ-1 was extended to route through Oversight (confirming an override is the only one, not merely the only one surfaced) and the Document reader, which is how stage verification actually proceeds.
- (note) One seam finding was already fixed before the lens reported: on-surface-faint at ~4.30:1 reflects the pre-contrast-fix token; current #8B9899 computes 6.11:1 on surface
- (event) Key screens rendered for Dashboard (mid-refresh) and Artifact view (memlog, stale) using the post-fix token values. Promoted .working/ keepers to mockups/ and linked from both spines with spines-win-on-conflict stated.
- (decision) Mock coverage confirmed: Dashboard and Artifact view mocked because layout drives behaviour there and the memlog timeline is the pattern most likely to be built wrong from a table. Oversight, Document reader, Comparison and No project are SPINE-ONLY by decision — the state matrix and Conditions-beyond-the-five now specify them tightly enough to build without a visual reference.
- (event) Input reconciliation: no-op and honestly so. imports/ is empty — Jamie supplied no visuals, sketches or brand material; the upstream PRD/spine/spec are inherited sources rather than user-supplied inputs to reconcile against.
- (event) Editorial pass on DESIGN.md: 40 recommendations (20 structure, 20 prose). Locked-eight section order verified preserved after the moves.
- (change) CONTRADICTION FIXED (self-inflicted, highest-impact editorial finding): section Colors forbade combining on-surface-faint with reduced opacity while section Components specified exactly that for the absent core card. Introduced when the contrast fix added the prohibition without updating Components. The token now carries the reduced weight alone.
- (change) Structure applied: Reference mockups moved from between Elevation and Motion to immediately after Brand & Style, so it front-loads the visual model instead of splitting the token sections; Motion now sits directly after Elevation & Depth. Locked eight untouched. Conformance record's revision-history paragraph cut as content that will rot — the memlog already holds it — keeping methodology, with its one operative rule hoisted into Don'ts as 'Don't re-reason a colour change. Recompute the ratios.'
- (change) Register drift removed from five passages that read as apology or changelog rather than specification: 'Two floors, because one was wrong', the mono-badge raise narrated as history, 'stated honestly', 'not estimated', and the nine-failures enumeration. A contract should not confess a previous state to a reader who never saw it.
- (change) Also fixed: typography role count seven -> eight; components inventory MECE failure (added core-artifact-card tokens, plus body entries for tile-raised and refresh-progress which had tokens but no prose); a normativity statement added (frontmatter is the contract, prose is rationale, token wins on conflict); the ambiguous 'The spines win on conflict' reworded; the one-accent-per-tile limit given a home in Colors where primary is defined; evidence-badge text-vs-border ambiguity resolved; four duplication clusters trimmed (elevation 3x->2x, signal closure, colour-alone, reduced-motion).
- (decision) DECLINED: the recommendation to change British 'colour' to US 'color' throughout. It is a locale choice, not an inconsistency — the 'colors' token key is fixed by the Google Labs design.md spec, so prose spelling and identifier spelling are different registers. Consistent with the same call made on the PRD earlier.
- (event) Editorial pass on EXPERIENCE.md: 17 structure + 20 prose recommendations. All four highest-impact findings were old-material/new-material collisions from the state-coverage revision.
- (change) SELF-CONTRADICTION FIXED: the Surface states gate read 'a rule that names no surface is not a specification for that surface' while six cross-cutting rules named no surface — one of them (Refresh failure) existing ONLY there. Reworded: cross-cutting rules bind by default, the matrix carries departures, and a surface's behaviour is its row read against the defaults. Refresh failure stated explicitly as a branch of Refreshing, not a sixth state.
- (change) 900px CONTRADICTION FIXED: stated three times across two documents, with Foundation claiming no layout is specified below it while Responsive specified the collapse order. Ownership split — DESIGN.md owns the breakpoint value, EXPERIENCE.md owns what collapses and in what order, Foundation points to it. Now once in each file.
- (change) String index completed: the Voice and tone table was framed as the canonical copy-review surface but the revision had added 9 strings that never reached it. All added, plus a stated convention (period for sentence-shaped, none for label-shaped, initial capital for badges, <placeholder> for substitution) and the rule that prose may quote a string but the table holds its authoritative form.
- (change) Also: absent-core-artifact rule merged into one owner (was specified twice in two vocabularies, the second crossing the visual boundary by referencing a DESIGN.md token); 'states' disambiguated throughout into signal states (four, closed) and surface states (five) — the drift most likely to cause a build error; view/page unified to surface; confidence unified to evidence tier; matrix Loading column dropped as constant; headings sentence-cased; Evidence and honesty patterns moved directly after Foundation as the document's thesis; Anti-patterns condensed to one-line pointers rather than cut, keeping the consolidated list a skimming builder uses.
- (decision) DECLINED again, consistently: British-to-US spelling. Locale choice, not inconsistency — token identifiers follow the design.md spec, prose follows the house voice. Same call as the PRD and DESIGN.md.
- (event) spines finalized
- (override) No project surface REMOVED from the UX contract during sprint planning (Jamie): the tool no longer serves when there is no project, so the surface count drops six to five. Side effect worth noting — IA closure is now clean, since that surface was the only one reached by error rather than intent.
- (decision) Font delivery resolved by Jamie during Story 1.2: system fallbacks only — no font files shipped, no CDN fetch. A CDN link would make the reader's browser request a third party (against NFR-11) and disclose tool usage; self-hosting would be the largest single addition to the startup budget (NFR-7). Typography tokens are unchanged: they already name IBM Plex first with platform fallbacks, so the contract stays honest. DESIGN.md now records that most readers see system faces and that the shared-metrics rationale holds only where Plex is installed.
- (override) TYPE FLOOR RULE amended from two classes to three by Jamie, during Story 1.2. The two-class version was internally inconsistent: it set the content floor at 13px while defining mono at 12px, a role DESIGN.md says carries file paths, timestamps and artifact IDs — content by any reading. mono was legal only by being miscalled a label. Now: content >=13px (body-dense), machine values >=12px (mono), labels and badges >=11px (tile-label, mono-badge). No token value changed; the rule was under-specified. Enforced by test: all three classes must be populated, since an empty class lets its floor pass vacuously.
