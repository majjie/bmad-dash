---
scope: bmad-dash — read-only CLI serving a local web dashboard over a BMAD project's artifacts
purpose: build-substrate
altitude: feature
updated: 2026-09-01T14:58
---

- (constraint) INHERITED from bmad-dash PRD (final, 70 active FRs, v1=C1..C6) — not to be re-decided: read-only invariant (NFR-1/2, enforceable by test); no telemetry or outbound requests (NFR-11); npx distribution, package bmad-dash (FR-45/46); user-initiated refresh, no filesystem watching (FR-37); current target project only, no cross-project scanning (FR-40); surface risk, never judge correctness — no LLM evaluation in v1 (section 2.3)
- (constraint) INHERITED: bind literal 127.0.0.1 only, never the name localhost nor 0.0.0.0 (NFR-9); serving confined to artifact tree, path traversal prevented (NFR-10); all content-derived path segments sanitized as untrusted on both filesystem and URL surfaces (NFR-17)
- (constraint) INHERITED: activity ordering hierarchy (FR-14) — uncommitted working tree > git commit time > memlog frontmatter updated (minute) > doc frontmatter updated (date-only) > run-folder date > mtime. Memlog entries carry NO per-entry timestamp (FR-66). Four incompatible timestamp formats must be normalized on read (FR-67).
- (constraint) INHERITED: artifact identification by 4-level precedence, filename never sufficient (FR-8); unresolved artifacts presented as unidentified (FR-69); BMAD-sharded documents adopt their existing shard boundaries as pages, heading-weight carving only for whole docs (FR-77)
- (event) Spine run opened from bmad-dash PRD; 7 withdrawn FR IDs (57-59, 61, 63-65) are retired and must not be revived
- (direction) Working mode: SPLIT (Jamie) — coach the calls that are genuinely his (paradigm, boundaries, what counts as an invariant); fast-path the research questions (runtime versions, starter, library choices) with web verification
- (direction) Altitude: feature — spine fixes the invariants keeping C1..C6 coherent as they become epics (stated to Jamie, uncontested)
- (direction) Audience includes future open-source contributors (Jamie) — spine must be readable by someone who never spoke to the author; a contributor-facing rendering is wanted at Finalize step 5
- (decision) READ LAYER = HYBRID (Jamie): project structure/index is a snapshot (option A) rebuilt on user-initiated refresh with a visible progress cue; individual file CONTENT is read on demand (option B). Binds C2/C3/C4/C5. Prevents: panels rendering from files in different states, and cold-start cost scaling with total bytes rather than file count.
- (question) CONFLICT FOUND: FR-28 full-text search is v1 (C5) and needs content from every file, which lazy content loading does not provide. Proposed resolution: two-phase snapshot — phase 1 structure/identification (fast, unblocks UI), phase 2 search index (background, reports through Jamie's progress cue, search honestly unavailable until complete). Awaiting Jamie.
- (constraint) FR-8 consequence on cost model: phase 1 cannot be a pure directory walk — identification precedence levels 2 and 3 require reading frontmatter and structural signature, so phase 1 head-reads every file. Cost is 'first chunk of every file', not 'list directories'.
- (question) OPEN: FR-47 currency rule under the hybrid — index currency (last refresh) and content currency (time of open) can disagree; needs an explicit rule rather than silence
- (question) OPEN: content cache lifetime — proposed parse-once-on-open, cache for life of current snapshot, discard on refresh, so refresh means one thing. Prevents re-parsing a 24k-word spec on every in-document page view (FR-25/26).
- (decision) Search conflict RESOLVED by removing the requirement, not by architecture: Jamie withdrew FR-28 upstream. Snapshot collapses to ONE phase — structure and identification only. Progress cue still warranted: phase 1 head-reads every file, which lags on a large _bmad-output tree.
- (decision) FR-47 currency rule (Jamie): on opening a document, the tool detects a mismatch between index currency and the file's actual current state and OFFERS A REFRESH. Turns a silent inconsistency into an actionable prompt rather than quietly correcting or silently disagreeing.
- (decision) Content cache lifetime (Jamie agreed): parse once on open, cache for the life of the current snapshot, discard on refresh — so 'refresh' means exactly one thing: everything you are looking at is replaced. Prevents re-parsing a 24k-word spec on every in-document page view.
- (event) Upstream PRD amended from the architecture run (FR-28 withdrawn) so source and spine do not diverge
- (decision) PARADIGM: hexagonal (ports and adapters) [ADOPTED by Jamie]. Chosen because it gives NFR-1 structural teeth — only the filesystem adapter may import node:fs, making read-only mechanically testable rather than conventional. Also makes identification precedence and timestamp normalization pure functions over data.
- (decision) RENDERING BOUNDARY [ADOPTED by Jamie]: server renders document content (markdown, sections, TOC, viewers) as HTML; client adds interaction on delivered markup (filtering, side-by-side selection, keyboard nav, modals, refresh progress). No client-side assembly of document content, no client router owning document URLs. Prevents two rendering models, permalinks working on one path only, client bundle growing to understand markdown.
- (decision) UI extensibility (Jamie: expects to extend the UI as BMAD grows): build step adopted from day one — esbuild, prebuilt assets shipped in the npm package so npx never builds. Preact for the interaction layer. Framework is SEED behind the rendering-boundary AD, so it can be replaced without touching document production.
- (version) Node 24 Active LTS (supported to 2028-04-30), floor Node 22 (maintenance). Node release model changes Oct 2026 to one major per year, all becoming LTS — pinning to an LTS line is more durable than usual. Verified via endoflife.date and nodejs.org release-schedule announcement.
- (version) markdown-it 14.x chosen over marked 12.x and remark 15.x/unified 11.x: marked returns HTML strings and cannot section a document, making FR-77 shard-aware paging and FR-26 TOC impossible; remark drags in the unified ecosystem; markdown-it's token stream carries heading levels at one dependency and is what VS Code uses, so contributors recognize it. Verified via pkgpulse 2026 comparison.
- (decision) Dependency minimalism is architectural, not taste: every dep is install latency on every npx invocation (NFR-7), package size, and supply-chain surface for a tool whose promise is not touching your project. Built-ins used for HTTP (node:http), CLI args (node:util parseArgs); git is shelled out to, matching BMAD's own git_evidence.py.
- (decision) AD candidate — SINGLE IDENTIFICATION AUTHORITY: artifact identity decided once, in one module, during the snapshot pass; no other unit re-derives it. Prevents the C2/C3 divergence where the interpreter and the activity ranker disagree about whether a folder is a run folder or a sharded document (FR-73).
- (decision) AD candidate — NORMALIZATION AT THE BOUNDARY: all timestamps converted to one internal representation on read; domain code never sees a raw BMAD timestamp string. Prevents the four incompatible formats (FR-67), three timezone-naive, leaking into comparison logic where two units would assume different zones.
- (decision) AD candidate — ORDERING EVIDENCE IS DATA: every activity item carries its ordering source tier and resolution as a field, not just a sorted position. Prevents FR-15 being implemented as UI decoration, and lets unorderable entries be grouped rather than given a false position.
- (decision) AD candidate — TYPED DEGRADATION: an artifact that fails to parse or identify yields a typed unparseable/unidentified value in the model, never an exception aborting the pass and never a silent omission. Prevents one unit throwing where another skips (NFR-3, NFR-4, FR-12, FR-69).
- (decision) AD candidate — SIGNAL AVAILABILITY IS RECORDED, NOT INFERRED: the model carries, per artifact family, which oversight signals were available and which absent; the oversight view reads that record. This is FR-76's floor made structural — without it 'nothing flagged' and 'nothing checked' are indistinguishable.
- (decision) AD candidate — ONE RESOLUTION PASS FOR LOCATION: project root discovered by walking up (FR-70) and artifact roots read from the target project's own config (FR-10), both resolved once at startup; every other unit consumes the resolved values. Prevents per-unit rediscovery disagreeing.
- (decision) AD candidate — UNTRUSTED PATH SEGMENTS: every path segment derived from project content passes one sanitizer before touching the filesystem or entering a URL, and every resolved path is confinement-checked against the artifact root. Prevents NFR-10/NFR-17 violations from unsanitized BMAD slugs reaching either surface.
- (decision) OPERATIONAL ENVELOPE: single local user process, ephemeral, no persistence of any kind (FR-61 withdrawn), no outbound network (NFR-11), bound to literal 127.0.0.1 (NFR-9). No deployment topology, no infra, no provider strategy — the envelope is 'one process on the user's machine for the life of the command'. Recorded explicitly so the dimension is decided rather than silent.
- (event) Input reconciliation (Finalize step 2) against the driving PRD: v1 coverage confirmed C1=9, C2=15, C3=14, C4=7, C5=4, C6=8 active FRs. Six NFRs had no spine home — NFR-6, 12, 13, 14, 15, 16.
- (decision) AD-13 added from reconciliation (NFR-6): convention-derived sources must fail visibly. BMAD defines status vocabularies in YAML comments; two units could each silently return empty on a shape change, showing nothing rather than a problem.
- (decision) AD-14 added from reconciliation (NFR-12): one canonical path representation, platform difference confined to the fs adapter. Artifact identity is keyed by path, so separator/case-sensitivity disagreement across Linux/macOS/Windows would fracture identity itself.
- (decision) AD-15 added from reconciliation (NFR-13): browser launch is best-effort and never gates serving — server binds and reports URL before any launch attempt. Prevents the tool being unusable over SSH, in containers, under WSL.
- (decision) Convention rows added for NFR-14/15 (single shared focus and keyboard model; meaning never in colour alone) and NFR-16 (refresh work bounded by file count; anything proportional to content size goes behind lazy loading). Judged cross-cutting conventions rather than ADs.
- (change) CORRECTION — stack pins were stale and one rationale was false. Verified directly against the npm registry: marked latest is 18.0.11 (memlog had said 12.x, six majors stale); markdown-it latest 15.0.1 with 14.3.1 now explicitly tagged v14-legacy; esbuild 0.28.2 (had 0.25.x); Preact 10.29.8 with 11.0.0-rc.1 in RC. Root cause: versions were taken from a comparison article instead of the registry.
- (change) FALSE RATIONALE RETRACTED: the claim that marked 'returns HTML strings and cannot section a document' is wrong — marked.lexer() returns heading tokens carrying depth, and marked.parser(tokens.slice(i)) renders an arbitrary token subset. Sectioning is achievable with marked. The earlier markdown-it justification does not stand.
- (decision) markdown-it 15.x RETAINED, on corrected grounds: the real advantage is token.map, which carries source LINE RANGES per token. That is what lets a section permalink (FR-27) and an open-in-editor target (FR-24) point at an exact line rather than a document. Secondary: v15 bundles TypeScript types. Trap recorded for implementers — heading level lives in token.tag/token.markup; token.level is nesting depth and reads 0 for h1/h2/h3 alike.
- (version) Node 24 leaves Active LTS 2026-10-20 (26 takes over under the new one-major-per-year model); floor 22 supported to 2027-04-30. parseArgs confirmed stable since Node 20.0.0 on both pinned lines.
- (version) Preact 11.0.0-rc.1 in RC with reworked hydration aimed at the SSR+islands seam AD-2 defines — evaluate before the client layer is built, not after
- (event) Reviewer gate returned: rubric walker THIN (16 findings, 1 critical, 7 high); tech-currency CHANGES REQUIRED (9 findings); adversarial-seams 'do not hand to implementation as-is' (12 constructed incompatible pairs). Two reviewers independently found the same critical and the same cross-document error — parent verified both against source.
- (change) CRITICAL RESOLVED (found independently by rubric walker and adversarial): AD-3 made the snapshot structure-only while AD-8 and C6 require content-level signals — mutually unsatisfiable, and literal compliance would report 'absent' for everything, the exact FR-76 failure. Root cause: withdrawing FR-28 removed SEARCH's need for content and was never re-checked against OVERSIGHT's. AD-3 rewritten on the extraction-versus-rendering axis: the scan reads bodies and extracts bounded facts including every C6 signal; what stays lazy is the rendering parse.
- (change) AD-1 TIGHTENED (adversarial): gating node:fs alone left node:child_process as an unguarded write path. Worse — git status --porcelain rewrites .git/index and repo-configured core.fsmonitor can execute commands from the scanned project. New AD-16: optional locks disabled, hook/monitor mechanisms neutralized by explicit config overrides, reporting commands only. A scanned repository must not be able to cause execution.
- (change) AD-5 CORRECTED: recording the timezone assumption is not fixing it — two compliant units could choose different zones. Naive sources are now interpreted in the tool machine's local zone, fixed, with the applied zone travelling for disclosure only.
- (change) AD-6 CORRECTED: 'resolution' had two competing definitions across AD-5/AD-6. Now one — the smallest time unit the source distinguishes — with timezone as a separate field. Added a tiebreak rule so a non-git project yields a usable feed instead of one large unordered bucket.
- (change) AD-8 state vocabulary aligned to four states (present/absent/unreadable/unchecked), previously contradicting the conventions table's triple. 'Artifact family' defined on ONE axis (producing BMAD workflow) — it was used by four ADs and defined by none, on two axes whose intersection was empty, so FR-17's filter and FR-76's coverage table could not be joined.
- (change) AD-9/AD-10 sequencing fixed: permitted roots are a SET, not one root; any project-content value that would become a root (story_location, which FR-74 shows may be absolute) passes the sanitizer and an admission check BEFORE joining it. Previously the composition root could admit an unsanitized path, giving one compliant unit an arbitrary-read primitive.
- (decision) New ADs from the gate: AD-17 a rendered page belongs to exactly one snapshot (snapshot identity; currency probe never cached) — closes four undetermined refresh-while-open points; AD-18 document/section URL grammar is a fixed identity contract across four units, un-deferred; AD-19 loopback server validates the Host header, since binding 127.0.0.1 restricts the network but not the browser.
- (decision) Triage: no blocking open questions remain. Stack pins carry a verification date and two watch items (Node 24 leaves Active LTS 2026-10-20; Preact 11 RC hydration rework) recorded in the Stack section rather than left as open questions.
- (event) Contributor-facing rendering published as an Artifact (Building bmad-dash) — organized by what each rule protects, every AD carrying its Prevents clause, plus recipes for adding a viewer, adding a signal, and reading from disk
- (event) spine finalized
- (override) AD-9 amended: project root IS the target path, recognized by _bmad and _bmad-output; resolution never walks. Second rule added confining the discovery scan to src/cli as a failure-message concern that must never become a resolution path — otherwise the nested-root ambiguity returns through the error path. Prevents clause updated: the divergence prevented is now a precedence rule someone has to remember.
- (override) LANGUAGE GAP CLOSED (Jamie, during Story 1.1 build): the Stack table named Node, markdown-it, yaml, Preact and esbuild but never named the LANGUAGE — a gap that survived three reviewers. It is TypeScript, compiled: sources are .ts, esbuild emits dist/, the package ships compiled output so npx never builds. TS and esbuild are devDependencies; the runtime dependency count stays zero.
- (decision) Native type stripping REJECTED for now (option A) in favour of compiling (option B): stripping is stable in Node 24.12 and default-on since 23.6, but requires raising the floor from 22, which is a supported LTS until 2027-04-30. The npx argument establishes that Node exists, not which version. Mitigation: tsconfig sets erasableSyntaxOnly so sources stay natively strippable and dropping the build later is a deletion, not a refactor. Verified against nodejs.org.
- (change) AD-1 amended: the import assertion reads .ts sources rather than compiled output, because the constraint lives in the source a contributor edits. Structural seed gains dist/.
- (decision) 405 method gate RATIFIED by Jamie and promoted into AD-19 as a second rule: write-shaped HTTP methods refused with 405 and an Allow header, after the Host check. Implemented and tested in Story 1.1 as behaviour beyond the spec's letter; recorded at the invariant level so later stories inherit it rather than re-deciding.
