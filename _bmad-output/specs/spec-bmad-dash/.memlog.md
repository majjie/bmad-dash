---
topic: bmad-dash — read-only CLI serving a local web dashboard over a BMAD project's artifacts, for human oversight of machine-generated work
updated: 2026-08-28T19:48
---

- (direction) Create from two final upstream artifacts: PRD (69 active FRs, v1 = C1-C6 = 59) and ARCHITECTURE-SPINE (19 ADs, hexagonal, feature altitude). Input is rich and pre-sorted, so extract directly — no elicitation.
- (decision) Slug is 'bmad-dash', not the inherited 'bmad'. Upstream run folders took their slug from project_name (the containing repo); the thing being specced is the product.
- (decision) PRD, addendum and ARCHITECTURE-SPINE are ADOPTED companions, not sources: each remains owned by its originating skill and downstream must still read them — the spine for the 19 ADs and its diagrams, the PRD for FR-level detail the kernel cannot hold, the addendum for glossary derivation when C9 arrives. sources: stays empty because nothing was fully absorbed with no remaining downstream value.
- (decision) Diagrams satisfy Spec Law rule 2 via the adopted spine companion, which already holds both mermaid diagrams — no spec-authored diagram companion needed.
- (capability) CAP-1 (from C1): launch against a target project and serve locally
- (capability) CAP-2 (from C2): interpret an arbitrary BMAD project's artifacts by content, tolerating version and layout variation
- (capability) CAP-3 (from C3): show what is recently active and what is central, with honest ordering evidence
- (capability) CAP-4 (from C4): read each artifact through a viewer suited to its shape
- (capability) CAP-5 (from C5): navigate a document too large to read as one page
- (capability) CAP-6 (from C6): surface the project's risk surface without judging correctness
- (decision) Spec-authored companion bmad-source-shapes.md created: multi-item catalog of measured BMAD v6.11.0 realities that bend implementation decisions — run folder families and collisions, slug semantics, timestamp formats, memlog weaknesses, marker coverage, files that are not what their name suggests, git behaviour, and what BMAD does not record. Warranted as a companion because it is a catalog, and it was previously scattered across two PRDs, six reviews and nineteen ADs.
- (constraint) Success signal taken verbatim from the PRD's operationalized gate: v1 succeeds when aggregate saved time+mistake cost across the user pool outweighs the cost of proceeding to v2 — assessed when the v2 question is asked, falsifiable
- (assumption) Node present on target machines (BMAD itself installs via npx); naive timestamps interpreted in the tool machine's local zone; audience is one role in two postures served by one interface
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8): PASS. Six capabilities each carry intent and success; intents state WHAT; all eleven constraints rule something out; eight non-goals; success signal is a falsifiable comparison rule; CAP-1..6 unique and stable; prose lean.
- (event) Self-validate pass 2 (preservation): PASS after one fix. Gap found and closed — FR-60 (user nominates two artifacts for side-by-side comparison) was represented only in Non-goals as what the tool does NOT do, losing the capability it does provide; added to CAP-6 success. All 59 v1 FRs, 17 NFRs, three user journeys, seven upstream-feedback gaps and the 19 ADs land in the kernel or a companion.
- (note) Wrapper-only content deliberately dropped: release-boundary rationale, journey-coverage prose, memlog audit narratives, and both upstream run histories — process record, not contract
