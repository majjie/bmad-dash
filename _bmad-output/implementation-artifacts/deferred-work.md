## Keys

One entry per finding. What is still open comes first — the block below, then
each `## Deferred from:` heading in the order the rounds happened — and
`## Resolved and closed` is last. Every entry carries `source_spec` and
`summary`; everything carried over from a review also carries `evidence`. The
rest are optional annotations, and every one of them is **append-only** — this
file exists so nothing is lost, so a claim that turns out to be wrong is struck
in place and corrected beside itself rather than edited away:

| Key | Required | What it holds |
|---|---|---|
| `source_spec` | yes | the spec the finding came from, or `none` when it was split out of planning |
| `summary` | yes | what is wrong, in the reviewer's terms |
| `evidence` | on review findings | where it was confirmed, and why it was deferred rather than fixed |
| `original` | no | the superseded text of a `summary`/`evidence` that has since been corrected, verbatim |
| `revisit` / `triggers` | no | the story or condition that makes it due |
| `findings_already_measured` | no | a decision taken during planning that the next story should not redo |
| `closed` | no | how it ended: RESOLVED, SUPERSEDED, or CLOSED BY DECISION, with the date |
| `mutations` | no | the mutation actually re-run against a resolution's test, and what failed |
| `correction` | no | what a `closed:` or `mutations:` line above got wrong, dated, left standing beside it |

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: The served page carries no hardening headers (nosniff, CSP, Referrer-Policy), and DNS-rebinding defence rests on Host alone with no Origin or Sec-Fetch-Site check.
  evidence: Real but premature — the page is a constant string today. Becomes load-bearing the moment Stories 1.12 and 2.1 render project content into it, and AD-19's rationale already anticipates the browser as the threat vector.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: The AD-1 gate does not cover ungated escape hatches to the filesystem (`node:module`, `node:worker_threads`, `node:vm`) or any outbound-network module, despite NFR-11 forbidding outbound requests entirely.
  evidence: A reviewer noted the gated set is narrower than the read-only, no-telemetry claim it enforces. Expanding the gate's remit is a scope decision rather than a patch, and gating network modules would also cover NFR-11, which currently has no automated enforcement anywhere. Postponed by the user with eyes open.
  revisit: Before Story 1.7, when markdown-it and yaml arrive. The gate reads source specifiers, so once a runtime dependency exists it could reach the network with nothing for the gate to see — a hole the gate cannot close retroactively. NFR-11 remains the only NFR with no automated enforcement until then.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: No socket-level limits configured — requestTimeout, headersTimeout, keepAliveTimeout, maxConnections all default — on a server the user leaves running all day.
  evidence: A reviewer noted this is also the knob that bounds the shutdown problem. Real, but a tuning decision rather than a defect in this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: `errorCode()` is duplicated across the adapter and the CLI, and several test helpers are re-implemented across test files.
  evidence: Roughly 80 duplicated lines with drift risk. Cleanup, not correctness; deferred so it does not compete with the correctness work in this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: Repository hygiene, consolidated: no README, no `keywords` in package.json, no CI workflow, no lint or format config, and no coverage instrumentation.
  evidence: Consolidates three overlapping entries recorded 2026-09-01, whose combined text is superseded by this one. Verified 2026-09-02: `.gitignore`, `LICENSE`, `license`, `author`, `repository` and a committed lockfile all now exist, so those parts are done and were making the list look longer than it is. What is genuinely absent is the five above. CI is the load-bearing one — nothing automated runs `tsc --noEmit` or the suite outside a contributor's own machine, and three review rounds each found gaps a green local suite could not see. It is not a pure win: it needs a decision on the OS and Node matrix. Note for whoever writes it — a CI container running as root now reports four *named skips* rather than an all-green suite (fixed 2026-09-02), so the run's `skipped` count is a signal worth failing or warning on.
  correction: 2026-09-02, second review round. Two things above are now out of date, and both matter to the CI decision this entry is about. **The skip signal is implemented**: `scripts/run-tests.ts` fails a run with any skipped test unless `BMAD_DASH_TEST_ALLOW_SKIPS` names how many are excused — the user chose failing over warning. And the count is no longer four: a root POSIX container now hits **five** named skips (`test/adapters/read.test.ts`, `test/cli/startup-order.test.ts`, `test/adapters/paths.test.ts`, and two in `test/cli/location.test.ts`), so a CI job running as root needs `BMAD_DASH_TEST_ALLOW_SKIPS=5` *and* someone to notice that five branches went unchecked — which is the argument for a non-root container rather than for an allowance.

## Deferred from: code review of spec-1-1-run-the-command-and-reach-a-served-page (2026-09-01)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: `process.cwd()` is called unguarded at `src/cli/index.ts:128`, so a deleted working directory throws ENOENT and produces a stack trace instead of a clean exit 1.
  evidence: Found by the edge-case reviewer. Exotic — it needs the cwd to be removed between shell prompt and invocation — and unrelated to this story's subject, but it is the one place the CLI touches the process environment without a guard while every other such touch (EPIPE, signals, argv) has one.

  note: Four other findings from this review were already recorded above and were not duplicated — the ungated networking modules, the missing hardening headers and socket timeouts, the `errorCode` duplication, and repository hygiene. The hygiene entries are now partly stale: `.gitignore`, `LICENSE`, and the `license` and `author` fields all exist, and a lockfile is committed. What remains absent is a README, `repository`/`keywords`, CI, lint/format config and coverage.

<!-- Story 1.1 code review, 2026-09-01: 27 findings. 3 high + 3 gate-related medium fixed;
     the 21 below deferred by decision so 1.1 could close. Locations preserved verbatim. -->

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) The request target is parsed with `split('?')`, not `new URL(..., base)`
  evidence: From the 2026-09-01 code review, located and verified there. Full text: The request target is parsed with `split('?')`, not `new URL(..., base)` [src/adapters/http/server.ts:329] — verified against the running compiled server: an RFC 7230 §5.3.2 absolute-form request, `GET http://127.0.0.1:<port>/ HTTP/1.1`, returns 404 instead of the page; percent-encoded and normaliza

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) Rejected request bodies are not drained
  evidence: From the 2026-09-01 code review, located and verified there. Full text: Rejected request bodies are not drained [src/adapters/http/server.ts:319, 325, 337] — no `request.resume()`, `request.destroy()` or body consumption anywhere in the module; the 403, 405 and 404 paths all respond and return with the stream unread. Current Node absorbs it, so nothing in the suite noti

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `Host` and `url` are not composed in bracketed form for IPv6
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `Host` and `url` are not composed in bracketed form for IPv6 [src/adapters/http/server.ts:128, 235] — for an IPv6 bind these produce `::1:8080` and `http://::1:8080/`; the correct authority is `[::1]:8080`, so every request would be 403'd and the URL would be unusable. Unreachable today because `LOO

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `process.exit()` is used where the task line requires `process.exitCode`
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `process.exit()` is used where the task line requires `process.exitCode` [src/cli/index.ts:117, 247] — both fire immediately after a `stderr(...)` write, and pipe writes are asynchronous, so `bmad-dash --bogus 2> file` can lose the diagnostic. At line 247 no server is running, so the change is safe;

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `close()` latches rejections as well as successes
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `close()` latches rejections as well as successes [src/adapters/http/server.ts:219] — `closing ??=` caches a rejected promise, so a transient close failure makes every later `close()` reject forever and the cached rejection can surface as an unhandled rejection. Only the success path is tested. Clea

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) The two "repeated signal" tests never deliver a second signal
  evidence: From the 2026-09-01 code review, located and verified there. Full text: The two "repeated signal" tests never deliver a second signal [test/server.test.ts:955-974] — `stopWith` (`test/server.test.ts:271`) kills only `if (child.exitCode === null && child.signalCode === null)`, and the first call resolves on `close`, after `exit` has set `exitCode`. Measured: first call d

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `identify()`'s 600ms timeout makes a security-relevant negative assertion pass vacuously
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `identify()`'s 600ms timeout makes a security-relevant negative assertion pass vacuously [test/server.test.ts:118] — a slow-but-reachable responder is classified `unreachable`, so "unreachable on every non-loopback interface" can pass for the wrong reason on a loaded host; interfaces are also probed

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `startup-order.test.ts` reports a 15s hang as the race defect it exists to detect
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `startup-order.test.ts` reports a 15s hang as the race defect it exists to detect [test/cli/startup-order.test.ts:145-152] — the timeout path resolves `{ code: null, signal: 'SIGKILL' }`, which lands in `killed` and is reported as "the readiness announcement raced the handler registration". The chil

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `run-tests.ts` calls `process.exit()` after writing the failure message, truncating the very output that message asks the reader to inspect
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `run-tests.ts` calls `process.exit()` after writing the failure message, truncating the very output that message asks the reader to inspect [scripts/run-tests.ts:40, 46, 65, 79] — it mirrors every child stdout chunk to its own stdout, then exits from the `close` handler; when stdout or stderr is a p

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (medium) `test/cli-entry.test.ts` errors rather than skips where symlink creation needs elevation
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `test/cli-entry.test.ts` errors rather than skips where symlink creation needs elevation [test/cli-entry.test.ts:204, 218, 233-234] — no `skip` appears anywhere in the file; every symlink test calls `await symlink(...)` bare. On Windows without Developer Mode or elevation this throws `EPERM` and rep

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) `readTotal` is coupled to the reporter's `ℹ` glyph, and counts subtests rather than test files. **The CRLF half of this finding was wrong and has been struck** — see evidence.
  original: Restored verbatim 2026-09-02 at the user's decision, because the no-loss contract has to hold inside this file and not only in git history. The correction below stands — the CRLF half really is wrong — but the text it corrected is this: "summary: (low) `readTotal` requires the `ℹ` glyph and a bare `\n`" / "evidence: From the 2026-09-01 code review, located and verified there. Full text: `readTotal` requires the `ℹ` glyph and a bare `\n` [scripts/test-run-policy.ts:84] — `/^ℹ tests (\d+)$/gm` fails on a CRLF reporter line or a re-encoded stream, turning a green suite into "could not determine how many tests ran". It also counts subtests, so the floor measures something different fro" (the review's own text was truncated there when it was first recorded).
  evidence: From the 2026-09-01 code review. Re-examined 2026-09-02 while clearing easy wins. The original claimed `/^ℹ tests (\d+)$/gm` "fails on a CRLF reporter line", which is false: under `/m` JavaScript treats `\r` as a line terminator, so `$` matches before it. Measured on all three of `\n`, `\r\n` and bare `\r` — every one matches, with and without a `\r?`. A "fix" was written, found to be a no-op whose test passed either way, and reverted; `test/tooling/run-policy.test.ts` now pins the real behaviour as a characterization test so nobody re-fixes it. What remains genuinely open is the other half: the match depends on the reporter emitting `ℹ tests <n>`, so a reporter change turns a green suite into "could not determine how many tests ran" — which is the safe direction but still a coupling — and the count includes subtests, so the floor measures something other than the number of test files. Inert today: the suite reports `suites 0` and uses no subtests.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) `engines: ">=22"` is unsatisfiable for AC1's own command
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `engines: ">=22"` is unsatisfiable for AC1's own command [package.json:12-14] — `npm test` runs `node scripts/run-tests.ts` and discovers `test/**/*.test.ts`, both needing unflagged type stripping (Node ≥ 22.18). The mismatch is documented only in a `"//"` string (`package.json:7`) that no tool read

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) Cross-pipe ordering on the three `Target:` assertions
  evidence: From the 2026-09-01 code review, located and verified there. Full text: Cross-pipe ordering on the three `Target:` assertions [test/server.test.ts:743, 756, 767] — `startCli` resolves on the first newline on **stdout**, then the test reads accumulated **stderr**. The child writes `Target:` first (`src/cli/index.ts:173`), but delivery order across two separate pipes to t

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-establish-the-visual-foundation.md`
  summary: (low) The contrast tests recompute the ratios DESIGN.md states and the pairings the system uses today, but nothing enumerates the *unused* pairings — a future story combining, say, `on-surface-faint` with a hover surface would get no warning until someone audits by hand.
  evidence: From the Story 1.2 review, 2026-09-01. Deferred rather than fixed because the full cross-product of 24 colours is 276 pairs, most of them meaningless, and asserting a threshold on pairs the design never uses would produce failures that are not defects. The right shape is a declared pairing table that Story 1.3 extends as it introduces surfaces — worth doing when there is more than one component to declare.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-build-the-components-every-surface-reuses.md`
  summary: (medium) Build `src/adapters/git/` and the git-availability probe that the project header reports, replacing the `Not checked` state Story 1.3 ships with.
  evidence: Split from Story 1.3 at the step-1 multi-goal check, 2026-09-01, by user decision. Two reasons. (1) It is the first and only consumer of `node:child_process` in the tree, governed by AD-16 — reporting commands only, optional locks disabled so no `.git/index` can be rewritten, and `core.fsmonitor` neutralized by explicit command-line override — which is the most security-sensitive code in the project so far and deserves a review round of its own rather than sharing one with tile padding and header markup. (2) `unchecked` is already a legitimate member of the four-state signal vocabulary, meaning "not examined at this altitude", so the header reports honestly in the meantime instead of carrying a placeholder that must later be found and removed. Also unblocks nothing: no Epic 1 story before 3.1 consumes git.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-build-the-components-every-surface-reuses.md`
  summary: (medium) `.tile-grid` ships `display: grid` with `grid-template-columns: 1fr`, so tiles stack in one column at every width. The dashboard's actual column behaviour and the 900px collapse are UX-DR22.
  evidence: From the Story 1.3 review, 2026-09-01. Not built here because a `minmax()` track needs a tile-width token the system does not define, and inventing one would put a value in the stylesheet that DESIGN.md does not carry — the exact thing the token rule forbids. The rule is commented as deliberately single-column so it does not read as finished. Story 1.12 is the first story with more than one tile to place, and is where the columns and the breakpoint belong.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-build-the-components-every-surface-reuses.md`
  summary: (low) `REFRESH_HREF` is hardcoded to `/`, so the header's refresh control will navigate to the Dashboard rather than re-requesting the current surface once other surfaces exist.
  evidence: From the Story 1.3 review, 2026-09-01. Correct today — the Dashboard is the only surface — and wrong the moment UX-DR15 adds a second. The fix is to pass the current surface's path into `projectHeader`, which is a one-parameter change best made by the story that creates the second surface, since that story can actually test it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-build-the-components-every-surface-reuses.md`
  summary: (low) `TileContent.html` is emitted verbatim while every other string in the render layer is escaped, so a caller passing project-derived text rather than composed markup would inject.
  evidence: From the Story 1.3 review, 2026-09-01. Now documented on the type and honoured at the only call site, which escapes before passing. A stronger fix is a branded `SafeHtml` type that only `escapeHtml` and the component builders can produce, making the unsafe path unrepresentable rather than merely documented — worth doing when there is more than one component composing another.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-establish-the-visual-foundation.md`
  summary: (low) RE-DEFERRED from Story 1.2 — the declared contrast pairing table that 1.2's entry said "Story 1.3 extends as it introduces surfaces" was not extended by Story 1.3.
  evidence: Re-recorded 2026-09-01 rather than left as a silently unmet trigger. Story 1.3 introduced three header pairings, all of which reuse colours the existing structural tests already sweep across the whole tonal ladder — `on-surface-variant` on every ladder level, which covers `.project-path` and `.project-signal`. So no new *pairing* went unchecked, and building the declared table would have been ceremony. The trigger stands for the first story that introduces a colour pairing the ladder sweep does not already cover.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-open-the-browser-but-never-depend-on-it.md`
  summary: (low) A repeated flag silently takes the last value — `--port 3000 --port 4000` binds 4000, and nothing warns. `parseArgs` is not configured with `multiple`, and the help text does not say last-wins.
  evidence: From the Story 1.4 review, 2026-09-01, verified against the built CLI. Left as-is because last-wins is the conventional shell behaviour and a reader who typed a flag twice most likely meant the second; making it an error risks refusing an invocation assembled by a wrapper script that appends a default. Worth revisiting if a flag ever gains a value where silently taking the last is dangerous rather than merely surprising.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-open-the-browser-but-never-depend-on-it.md`
  summary: (low) The help text documents no exit codes, and does not mention that `--` ends option parsing — so `bmad-dash -- --no-open` resolving a path literally named `--no-open` is undiscoverable from the tool itself.
  evidence: From the Story 1.4 review, 2026-09-01, both verified. The CLI deliberately distinguishes exit 2 (you typed it wrong) from exit 1 (it could not start), which is useful to a script author and currently documented nowhere. Deferred rather than added now because the right place is probably a `--help` section that grows with the flag surface, and the surface is four flags old.

- source_spec: none
  summary: (high) Read the target project's own configuration to locate artifact roots (FR-10) — `_bmad/bmm/config.yaml` and its user override — substituting `{project-root}`, admitting each resolved root to the permitted-root set only after an out-of-tree check, and reporting a malformed config rather than partially applying it. Should land as its own story before Story 1.7, alongside the safe tree walk.
  evidence: Split from Story 1.5 at the step-1 checkpoint, 2026-09-02, by user decision, because the narrowed 1.5 (root resolution, canonical paths, reading surface) is already at the token ceiling. Until this lands the artifact roots stay at their documented defaults, which is a known-wrong state carried deliberately — FR-10 exists precisely because a project may put them elsewhere.
  findings_already_measured: The parser decision was made and verified during 1.5 planning, so the next story should not redo it. `yaml@2.9.0` (ISC, no dependencies of its own, ships types) is to be a **devDependency bundled by esbuild**, not a runtime dependency — user decision, and the architecture spine records why. Measured: its node export is CommonJS and dynamically requires `process` and `buffer`, so an ESM bundle throws `Dynamic require of "process" is not supported` at import unless the build carries a banner such as `--banner:js='import{createRequire as __cr}from "node:module";const require=__cr(import.meta.url);'`. With that banner, parsing of flat scalars, one level of nesting and inline sequences is correct; the only dynamic requires in the output are those two builtins; and there is no `node:http`, `node:https`, `node:net`, `node:fs`, `node:child_process` or `fetch` anywhere in it. The bundle grows from about 34KB to about 264KB. `LICENSE-THIRD-PARTY` carrying yaml's ISC text must ship, taking the tarball from three files to four. Measured shapes it must handle: `config.yaml` is flat `key: scalar`; `sprint-status.yaml` has one level of nesting; artifact frontmatter has scalars and inline sequences.
  status: DEFERRED INDEFINITELY 2026-09-02, by user decision — no longer queued before Story 1.7, and no longer the story that widens the permitted-root set, because there is no set: AD-9 was narrowed to one root in the same decision. **What this costs, stated so it is not discovered later:** FR-10 is unmet, so artifact roots stay at their documented defaults and a project that puts them elsewhere is read at the wrong paths. That is a known-wrong state carried deliberately. The `yaml` bundling decision below stays measured and valid if this is ever revived; NFR-11's trigger does not fire while it is deferred, because no third-party code executes at runtime.
  triggers: NFR-11's deferred automated enforcement comes due with this story, not before — a gate that reads source specifiers proves nothing once third-party code executes at runtime, so it must assert against `dist/` instead.

## Deferred from: review of the deferred-findings cleanup batch (2026-09-02)

Found while verifying the batch that put `scripts/check-tasks.ts` under test.
The first two were opened by that work; the three `tickedTargets` entries came
out of the review round on it and are deferred by decision, not by omission.

- source_spec: `_bmad-output/implementation-artifacts/spec-land-deferred-findings-cleanup.md`
  summary: (low) The stale-`.js` guard is still scoped to `src/`, so a compiled leftover under `scripts/` or `web/` is invisible to it — `test/architecture.test.ts:134-138` filters `file.startsWith('src/')` while the gate scans three roots.
  evidence: Found 2026-09-02 while re-verifying the closed "Stale `.mjs` references" entry, whose closing note claims this consequence "no longer exists in the code". It does. The premise that justified the scoping ("`scripts/` is plain `.mjs` tooling") was deleted from the comment; the scoping itself was left. Harmless today because `scripts/` holds three `.ts` files and `web/` is empty, and it is a one-token change — but it is a guard narrower than the invariant it is named for, which is the shape of defect this list keeps recording.

- source_spec: `_bmad-output/implementation-artifacts/spec-land-deferred-findings-cleanup.md`
  summary: (low) `changedFiles` in `scripts/check-tasks.ts` carries two latches for one invariant, and only one of them is observable end to end: `--full-name` and `-c diff.relative=false` are both redundant while the commands also run with `cwd: repoRoot`.
  evidence: Measured 2026-09-02 with git 2.43.0 while writing `test/tooling/check-tasks.test.ts`, not reasoned about. Run at the repository root, `ls-files --others` and `diff --name-only` are already repo-relative, so reverting either flag changes no verdict from any cwd — the spec's acceptance criterion for that mutation cannot be met by a black-box run. It is met instead by `both list commands are pinned repo-relative in the argv the checker issues`, which puts a logging `git` shim on PATH and asserts the argv. That test earns its keep (removing `cwd: repoRoot` *is* caught end to end, by two other tests, so the flags are the half that could vanish silently) but it is a whiter box than the rest of the suite. Two of its rows — the shim and the empty-PATH missing-git row — name-skip on `win32`, so the flag pinning and the ENOENT branch of `git()` are unverified on Windows. Comes due with the CI matrix decision in the "Repository hygiene, consolidated" entry.

- source_spec: `_bmad-output/implementation-artifacts/spec-land-deferred-findings-cleanup.md`
  summary: (medium) `tickedTargets` in `scripts/check-tasks.ts` treats any leading backticked token containing a `.` as a path, so a task line that leads with an identifier — `` `t.skip` ``, `` `Promise.race` ``, `` `diff.relative=false` `` — is checked as a filename, misses, and exits 1 on honest work.
  evidence: Found 2026-09-02 in the review of the batch that put the checker under test. The heuristic is "first backticked token, if it contains `/` or `.`", which cannot distinguish `src/a.ts` from `a.b`. This project's own task lines lead with a path by convention, so it is latent rather than live — but the failure it produces is the false accusation the checker's whole value depends on not producing, and the false accusation is what teaches a reader to stop trusting the guard. **Deferred rather than patched, deliberately:** every tightening trades one error for the other. Requiring a `/` misses a ticked `package.json` or `README.md` at the root; requiring a known extension needs a list that will be wrong; asking git whether the token is a path makes the heuristic depend on the diff it is checking against. Choosing among those is a decision about which error to prefer, and it should be made once, on purpose, rather than inside a cleanup batch.

- source_spec: `_bmad-output/implementation-artifacts/spec-land-deferred-findings-cleanup.md`
  summary: (medium) `tickedTargets` matches only unindented, lowercase `- [x] `, so a nested sub-task or a `- [X] ` tick is invisible to the guard — it reports "every ticked task names a file the diff touches" without having looked at them.
  evidence: Found 2026-09-02 in the same review. `line.startsWith('- [x] ')` fails on `  - [x] ` and on `- [X] `, both of which render as a ticked checkbox in every markdown tool and both of which a human will write. This is precisely the "a checker that finds nothing to check has become a no-op" shape the file's own header names as its reason to exist — and worse than the no-op case, because the `targets.length === 0` guard only fires when *nothing* matched: a spec with one top-level tick and six indented ones passes with one checked. Deferred with the entry above because the two share the parser and should be fixed in one pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-land-deferred-findings-cleanup.md`
  summary: (low) The baseline-verification block in `scripts/check-tasks.ts` runs `rev-parse --verify` with `stdio: 'ignore'`, so *any* failure is reported as "Rebased or amended since the spec was written?" — including an unreadable `.git`, a corrupt object database, or a sha that names a tree rather than a commit.
  evidence: Found 2026-09-02 in the same review. The message sends the reader to edit the spec's frontmatter when the actual fault is in the repository, which is the same class of misdirection the `git()` wrapper's exit-2 separation exists to prevent — one level finer. Low because a rebased baseline really is the overwhelmingly common cause and the exit code (2, a broken check) is correct either way; the fix is to capture stderr and include it, which is a small change to a block that currently has a deliberate reason to `catch`.


- source_spec: `_bmad-output/implementation-artifacts/spec-land-deferred-findings-cleanup.md`
  summary: (medium) Roughly twenty `symlink()` calls across seven test files have no `win32` guard and pass no link type, so on Windows without elevation they throw `EPERM` and the tests **error** rather than skip.
  evidence: Counted 2026-09-02 during the second review round, in `test/architecture.test.ts`, `test/adapters/read.test.ts`, `test/adapters/realpath.test.ts`, `test/adapters/paths.test.ts`, `test/cli-entry.test.ts` and `test/cli/location.test.ts`. Pre-existing, and only one instance was in scope for that round — the new characterization test in `paths.test.ts`, which is now guarded and passes `'dir'`. Recorded because the fail-on-skip check landed in the same round and changes what this costs: a skip is now a red build that names what went unchecked, whereas an `EPERM` error is a red build that names the wrong thing. Not urgent — nothing has run this suite on Windows — but it comes due with the OS matrix in "Repository hygiene, consolidated", and it should be one pass over all seven files rather than six separate ones.
  findings_already_measured: Story 1.6a, 2026-09-02, built the missing half and used it in two of the seven files, so the next person inherits a helper rather than a decision. `test/support/tree.ts` now owns both platform questions and answers them by **measurement**: `symlinksAvailable()` creates a probe link in a throwaway directory (elevated Windows can, unelevated cannot, and no `process.platform` check separates those), and `deniableDirectories()` is false on Windows and for root. `makeTree` refuses to build a link when the probe says no, naming the guard the caller is missing, and every link it makes carries a required `type`. `whileDenied` scopes a `0o000` denial to a body and restores **the mode it found**, in a `finally` rather than an `after` hook — the hook ordering is why a denied directory cannot be a declarative fixture node. **What remains** is the five files that still call `symlink()` inline and unguarded: `test/architecture.test.ts`, `test/adapters/realpath.test.ts`, `test/adapters/paths.test.ts`, `test/cli-entry.test.ts` and `test/cli/location.test.ts`. `test/adapters/read.test.ts` and `test/adapters/walk.test.ts` are done. The remaining work is mechanical now — guard with `symlinksAvailable()`, pass a link type — and is still one pass rather than five.

## Deferred from: review round 1 of spec-1-6a-walk-the-artifact-tree-safely (2026-09-02)

Three findings deferred by decision. Every other finding in that round —
twenty of them — was applied in the round itself. One of the three, the walk's
empty importer set, closed on 2026-09-02 and sits under `## Resolved and
closed` with its resolution beside it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6a-walk-the-artifact-tree-safely.md`
  summary: (medium) A Windows directory junction is a reparse point that can target outside the permitted root and answers **false** to `isSymbolicLink()`, so the walk's no-probe fast path may admit one as an ordinary directory and descend it.
  evidence: Raised in the round, 2026-09-02, and **unfalsified rather than verified** — nothing has ever run this suite on Windows, so neither the defect nor its absence is measured. What is known: `Dirent.isSymbolicLink()` is documented false for junctions, and the fast path in `ConfinedReader.#child` trusts a `Dirent` that positively answers `isDirectory()`. Two things narrow it. The rule was tightened in this round to trust only a *positive* `isDirectory()`/`isFile()` answer (`trustedKind`, unit-tested against a `DT_UNKNOWN`-shaped entry), so an entry the filesystem is unsure about is always probed. And confinement does not depend on the probe at all: every child goes through `canonicalWithResolution` first, and `realpathSync.native` on Windows does resolve junctions — so a junction leaving the root should be refused at the `confinement` stage regardless of what the `Dirent` said. The residual risk is that "should" is a reading of the API rather than a measurement.
  triggers: The OS matrix in "Repository hygiene, consolidated". The first Windows run must include a junction fixture (`mklink /J`) asserting the entry is `unchecked` at the `confinement` stage and never descended.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6a-walk-the-artifact-tree-safely.md`
  summary: (medium) `test/support/gate.ts:337` is still the defective walk this story was written against — it rethrows on `EACCES`, so one unreadable directory ends the whole AD-1 gate pass; it has no depth, breadth or total bound; and it still has no unit test of its own.
  evidence: Confirmed unchanged 2026-09-02. Story 1.6a's spec cites it five times as its counter-example and did not touch it, deliberately: the spec's task list does not include it, and rewriting the gate's own scanner inside a story about the production walk would put the suite's enforcement mechanism and the code it enforces in one review round. What has changed is the cost of leaving it: a correct implementation now exists beside it to model — `src/adapters/fs/walk.ts` — so the fix is a port rather than a design, and the gate's own header already records the two defects a unit test would have caught. Note the gate is not confined and must not become so: it scans repository sources, not a project root, so it wants the bounds and the failure-as-value rule, not `ConfinedReader`.
  triggers: The next change to `test/support/gate.ts` for any reason, or the first time a contributor runs the suite where a source directory is unreadable.

## Deferred from: review round 1 of spec-1-6-fail-with-the-command-that-would-have-worked (2026-09-02)

Four findings deferred by decision rather than by omission. Every other finding
in that round was applied in the round itself.

correction: 2026-09-02, Story 1.9. The line above is left standing and is now
wrong by one: the untyped-`reason` finding it counted — `Listing`'s raw OS
message — was closed by decision in Story 1.9 and has moved to `## Resolved and
closed` with its closing note and a correction of its own evidence. **Three
entries** remain in this block. What that entry got wrong is worth reading
before trusting a deferral's urgency: it recorded the reason as having become
user-facing, and the code it named discards it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-fail-with-the-command-that-would-have-worked.md`
  summary: (low) The refusal path canonicalizes the target twice. `resolveLocation` computes the canonical root and discards it on every failure variant, so `suggestInvocations` re-derives it with a second `realpath`.
  evidence: Confirmed in the round, 2026-09-02. Costs one syscall on a path that is already exiting, so it is not a performance finding. What makes it worth recording is the coupling: the refusal message and the scan's own "ancestors of `<path>`" sentence are built from two independently canonicalized values that agree by coincidence rather than by construction. Carrying the canonical root on the `ok: false` variant would make them agree by definition. Deferred because widening the `Location` failure shape touches Story 1.5's frozen surface and every consumer of it, which is not a review-round patch.
  triggers: The first story that adds a second consumer of a refusal's path, or any change to `Location`'s failure variant for another reason.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-fail-with-the-command-that-would-have-worked.md`
  summary: (low) `readdirSync` materializes every entry before the breadth cap applies, so a directory of millions of entries spikes memory. `opendirSync` plus `readSync` would stream it.
  evidence: Confirmed in the round, 2026-09-02, and stated in `src/adapters/fs/list.ts`'s own header rather than left implicit. The cap bounds the *directories* the scan probes and descends into, which is where the cost is; it does not bound the entry list `readdir` builds in order to answer at all. Deferred because streaming means owning a `Dir` handle and its close on every failure path, which is more machinery than a bounded failure message justifies today.
  revisit: If the scan is ever pointed at a directory where this is real — a mail spool, a content-addressed store — or if the listing adapter gains a second caller with a larger appetite.
  findings_already_measured: The second half of that trigger fired on 2026-09-02, and Story 1.6a decided it rather than rediscovering it. `ConfinedReader.childrenOf` is the larger-appetite caller — it recurses, through `src/adapters/fs/walk.ts` — and it **keeps the materialization**, deliberately. Two reasons, both recorded at `childrenOf` itself. The failure paths are exactly what that module is for, and `opendirSync` means owning a `Dir` handle and closing it on every one of them; a handle leaked on an `EACCES` part-way through an iteration is a worse defect in a long-lived server process than the memory spike it avoids. And the cap does more work here than it does in `list.ts`: names are sorted and capped **before any child is probed**, so it bounds syscalls rather than only array length. The entry list `readdirSync` builds is still unbounded. **Correction, same day, from the review round:** the first version of this note was wrong by omission — that implementation added a mapped names array, a sort of it, and a lookup `Map`, of which only `limit` entries were ever used, so the footprint of the allocation this note defends keeping had tripled. Fixed rather than re-recorded: `childrenOf` now filters and sorts the `Dirent[]` in place and slices it, so nothing is allocated on top of the array `readdir` already built. This stays open for the case the `revisit` names — a walk pointed at a mail spool or a content-addressed store — and it is now open against two callers instead of one.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-fail-with-the-command-that-would-have-worked.md`
  summary: (medium) `scripts/check-tasks.ts` verifies ticked-task -> touched-file but never the reverse, so a diff can touch files that no ticked task names and the check still exits 0.
  evidence: Confirmed in the round, 2026-09-02, on this story's own diff: eight ticked tasks against an eleven-file diff exited 0 with no `MISS`, and three of those files were edits nothing in the spec accounted for. The tool exists because judgement failed at exactly this step, and it currently catches a tick without work while missing work without a tick — the same class of defect from the other side. Deferred because deciding what counts as an unaccounted file is a policy question (planning artifacts? lockfiles? a fixture?) and guessing it wrong makes the check noisy, which is how a guard gets skipped.
  triggers: The next change to `scripts/check-tasks.ts` for any reason; it should not gain a third feature before this second half exists.
  findings_already_measured: Demonstrated a second time on 2026-09-02, by Story 1.7's own diff and in the review round of it. Two files were edited that no ticked task names: `_bmad-output/implementation-artifacts/sprint-status.yaml` (the workflow's own status transition) and `src/adapters/fs/walk.ts` (one word — `childRelative` was exported so the pass would stop carrying a verbatim copy of it). `check-tasks.ts` exited 0 on both, because it only ever asks tick→touched. Neither is wrong, and that is the point the policy question turns on: the check cannot distinguish "a file the story legitimately touched" from "a file nobody accounted for" without a rule about what counts, and Story 1.7 now supplies two concrete cases for whoever writes that rule — a workflow-owned tracking file, and a one-line export in a module the story composes but has no task line for. Third instance, 2026-09-02, Story 1.8: `check-tasks.ts` exited 0 over a diff of **twelve** files where nine were accounted for, and the three unaccounted ones repeat 1.7's two shapes exactly — `_bmad-output/implementation-artifacts/sprint-status.yaml` (the workflow's own status transition again), `src/domain/document.ts` and `test/domain/document.test.ts` (both ticked, so accounted), and `src/domain/identity.ts`, which gained two exported helpers so the document model would stop carrying its own copy of "what counts as markdown" — the same one-line-export-in-a-composed-module shape as 1.7's `childRelative`. The recurrence is itself the finding: whatever rule gets written has to name the workflow's own tracking file explicitly, because it will be in every story's diff and no story will ever have a task line for it, and it has to decide whether a shared helper extracted *into* a module the story composes needs its own task line. Fourth instance, 2026-09-02, Story 1.9, and the same two shapes again: `_bmad-output/implementation-artifacts/sprint-status.yaml` (the workflow's own status transition, now in four consecutive diffs), plus `src/adapters/fs/read.ts` and `test/adapters/read.test.ts` — the reading adapter gained a typed `stage` on `readText`'s failure so the pass could name the stage a read stopped at, which is that story's whole contract, and no task line named the adapter. That is the shared-helper shape widened from an export to a return type, and it is the case that makes the question sharper rather than easier: the change is *why* the ticked work is possible, so a rule that flags it as unaccounted would flag honest work, and a rule that exempts "a module the story composes" exempts most of `src/`.

## Deferred from: implementation of spec-1-7-identify-what-each-artifact-is (2026-09-02)

Six entries. The first three are **decisions taken during implementation** that
the story's own spec asked to be recorded, because each is a reading of a
requirement rather than a mechanical consequence of it, and each will otherwise
be re-litigated by whichever story touches identity next. The last three are
findings deferred by decision in the review round of 2026-09-02 — the pass's
empty importer set, and the two holes in "the children are what the directory
holds". Every other finding in that round was applied in the round itself.

correction: 2026-09-02, Story 1.8. The paragraph above is left standing and is
now wrong in two places, which this corrects rather than edits away. It says
**six entries** and says the last three are "the pass's empty importer set, and
the two holes in 'the children are what the directory holds'". The
alias-suppression hole is resolved and has moved to `## Resolved and closed`
with its closing note, so this block holds **five** entries, of which the last
two are the empty importer set and the *one* remaining hole — the skip policy's
filtered listing. The resolved half is now a finding about what this project
gets wrong, which is what that section is for.

correction: 2026-09-02, Story 1.9. And now wrong by one more, for the same
reason: the FR-12/FR-69 **collision** entry was resolved by Story 1.9 — the
story its `triggers` line named — and has moved to `## Resolved and closed`
with the decision and its reasoning. This block holds **four** entries.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-identify-what-each-artifact-is.md`
  summary: DECISION — FR-8's level 4 (filename) **resolves**, and never at `certain` confidence. Recorded because FR-8 contains two halves that cannot both be taken literally.
  evidence: FR-8 says the precedence is "applied in order and stopping at the first that resolves" and, in the same requirement, that "filename alone is never sufficient". Read as "a filename does not resolve at all", level 4 is not a precedence level and FR-8's own numbered list has three entries — which contradicts FR-69's copy in `EXPERIENCE.md:92`, `Not identified. Tried: config path, frontmatter, structure, filename.`, naming four. Read as "level 4 resolves like any other", `prd.md` is as certain as a declared `type:`, which is the false confidence the second half forbids. The implemented reading is the third: level 4 resolves *and* carries `likely`, which is exactly what FR-69's "confidence is displayed where it is below certain" exists to render. Chosen by the user, stated in `src/domain/identity.ts`'s header, and pinned from both sides in `test/domain/identity.test.ts` — every filename verdict is asserted `likely` and every other level's is asserted `certain`, so a mutation in either direction fails.
  findings_already_measured: `Confidence` is a **two**-value vocabulary (`certain`, `likely`). No third value was invented: FR-69 asks one question — is this below certain, and therefore something to display — and a ladder of gradations would be a UI vocabulary nobody has specified. A story that needs a third should add it with the surface that renders it. **The Story 1.9 half of the trigger below did not fire**, added 2026-09-02 to this same key rather than as a second one: Story 1.9 landed with no rendering at all — `src/render/page.ts` is still the placeholder surface and nothing imports the inventory pass — so the below-certain state is displayed nowhere yet, and Story 1.12 is still the first surface. What 1.9 did touch is next to this and left it alone deliberately: `Confidence` is unchanged at two values, and readability was added as a **separate signal** (`src/domain/signal.ts`) rather than as a third confidence value or a fifth verdict outcome, which is the widening this entry's second trigger exists to catch.
  triggers: Story 1.9, which renders the below-certain state, and any story that adds a fifth precedence level or a third confidence value.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-identify-what-each-artifact-is.md`
  summary: DECISION — the seven artifact roots and the seven run-folder patterns are **measured against a BMAD v6.11.0 install, not specified anywhere**, and level 1 is therefore called `config path` while reading defaults rather than the project's own configuration.
  evidence: Measured 2026-09-02 and encoded as `ARTIFACT_ROOTS` and `RUN_FOLDER_PATTERNS` in `src/domain/identity.ts`. Two findings inside it are the ones worth keeping. **Two of the seven families do not live under `planning-artifacts`**: `specs` and `forge` sit directly under `_bmad-output`, so a level-1 rule keyed on `planning-artifacts` alone misses two families out of seven — asserted directly in `test/domain/identity.test.ts` so the shape of the list is a claim rather than a coincidence. And **the family-to-pattern mapping for the two dateless run folders is stated nowhere in BMAD**: `spec-{slug}` was measured to be the spec family and the bare `{slug}` the forge family, which matters because `{slug}` matches every directory name and therefore carries **no name signal at all** — a forge run is recognizable by location alone, which is the identity consequence of FR-72's ordering one. Sources: `_bmad/bmm/config.yaml:7-8` for `output_folder` and `planning_artifacts`, BMAD's own help tables for the per-skill output paths, and the installed `bmad-deep-recon` for the six research type packs that form `{research_type}`.
  findings_already_measured: A family's own directory (`…/prds`) is recorded as `shape: 'container'` rather than left unidentified. Without that rule the inventory carries seven permanent "Not identified" rows for the tool's own layout, which is noise presented as a failure. It is a rule the spec's matrix did not ask for and it is asserted per-root, so removing it is visible. Also measured and corrected in the round: level 3's structural read over a directory's children is restricted to **markdown children matched exactly**, because without that restriction `_bmad-output` identifies as a spec on the strength of *containing a directory called `specs`*.
  triggers: FR-10, whenever it is un-deferred — at which point level 1 reads the project's declared roots and this table becomes the fallback rather than the answer. Until then a project that relocates its output folder or its artifact roots is read at the wrong paths, and that is FR-10's cost, paid here.
  correction: 2026-09-02, review round. The `summary` above says "seven artifact roots" and that was **wrong by omission**, left standing here rather than edited away. `_bmad/bmm/config.yaml:7-8` declares *two* peer output roots — `planning_artifacts` **and** `implementation_artifacts` — and the first draft of the story's Code Map listed only the first, so the implementation was faithful to a spec that was short one root. Measured consequence: every `spec-1-*.md`, `epic-1-context.md`, `deferred-work.md` and `sprint-status.yaml` in this project's own tree returned `location: no-signal`, several fully `unidentified`, about a third of a real output tree. Resolved in iteration 1 by amend-and-extend: `_bmad-output/implementation-artifacts` is now a level-1 root of a second kind — one that holds several families and tells them apart by document name *within* the root, with a residual family so level 1 is total over a declared root — and the family vocabulary widened from FR-11's seven to eleven, because FR-11 constrains run folders rather than the artifact universe. `RUN_FOLDER_FAMILIES` still holds exactly the seven and is what every run-folder pattern resolves to. Two further measured facts came out of the same round and are recorded here rather than rediscovered: BMAD's output **layout** (`_bmad-output` and the two declared roots) is not read from its children at all, because `planning-artifacts` contains an `epics.md` and was being identified as an epics artifact for holding one — the same false-positive class as `_bmad-output` identifying as a spec; and those three directories are the only rows in this repository's own inventory that resolve to no family, which `test/cli/inventory.test.ts` now asserts end to end by enumerating them.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-identify-what-each-artifact-is.md`
  summary: (low) `src/cli/inventory.ts` — the identification pass — has an asserted-**empty** importer set and no consumer, so it is the same shape the walk was in one level up: a module held in place by a test that says nothing imports it.
  evidence: Recorded 2026-09-02 by decision, and deliberately in the same words as the entry it inherits from, so the pattern is visible rather than rediscovered. The pass is the walk's consumer, which is what closed the walk's own entry; nothing yet renders what the pass produces. Story 1.12 ("See the project inventory") is the first surface, and Stories 1.8 to 1.11 all consume this story's verdict on the way there. `esbuild` bundles from `src/cli/index.ts`, so nothing unreachable reaches `dist/` — the cost is in the repository, not the tarball. The alternative was wiring the pass into the composition root now, which would mean either serving an inventory no story has designed or calling it and discarding the result, and the discarded-result shape is the unconsumed surface this project has already been caught with three times.
  triggers: Story 1.12. If it lands without importing the pass, that is a finding about 1.12, not about this entry.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-identify-what-each-artifact-is.md`
  summary: (low) A child the **skip policy** removed also vanishes from the listing handed to the domain, and the listing is still reported `available` — so level 3 draws a conclusion from a filtered set without being told it was filtered.
  evidence: Confirmed 2026-09-02 in the review round and answered by decision rather than by code, with the reasoning recorded at both `src/cli/inventory.ts`'s header and `test/cli/inventory.test.ts`'s row for it. Marking the whole listing `unavailable` because one child was excluded would silence level 3 for any run folder that happened to contain a dependency directory, which is the worse trade; what makes the current answer safe is narrow and measured rather than structural — the only name skipped below the output folder is `node_modules`, which is neither a family document nor an `index.md`. The safety is therefore a property of `SKIPPED_NAMES` having one entry, not of the design. A third state on `Listing` — available-but-filtered — is the honest fix and is a domain vocabulary change.
  triggers: Any addition to `SKIPPED_NAMES`, or any skip rule that could remove a markdown file. That is the moment the measured safety stops holding.

## Deferred from: implementation of spec-1-8-handle-whole-and-sharded-documents-alike (2026-09-02)

Five entries. The first four are not review findings: they are the things the
story's own spec required be **recorded rather than silently invented**. Three
are limits of what any source actually specifies about sharded documents —
`review-state-coverage.md:113` calls them "a requirement with no design" — and
the fourth is the consequence of there being no sharded document in this
repository to measure against. Epic 2 owns shard boundaries (FR-77, Stories
2.9–2.11) and should overrule these rather than inherit them as decisions
someone believes were specified. The fifth is the **cost of this story's own
fix**, added in the review round of 2026-09-02 because it was stated only in a
code comment and a cost nobody can find is a cost nobody accounts for.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-handle-whole-and-sharded-documents-alike.md`
  summary: DECISION, invented not specified — a sharded document's part order is `index.md` first, then the remaining markdown children **lexically by code unit**. No BMAD source states an order.
  evidence: Recorded 2026-09-02 while implementing `src/domain/document.ts`. What was searched for and not found: a manifest convention, an ordering inside `index.md`, or any statement of shard order in `prd.md` (FR-9, FR-73), `ARCHITECTURE-SPINE.md` (AD-3, AD-4, AD-18) or `EXPERIENCE.md`, whose word "sharded" appears only in the conditions list. BMAD's own discovery uses a `*prd*/index.md` glob and asks a human where it cannot tell a run folder from a sharded document, so it has no ordering to copy either. Chosen because it is the only order that is deterministic, locale-independent (`localeCompare` was deliberately rejected: a part order that varied by machine locale would give two developers different page numbers for one document, which is what AD-18 exists to prevent) and puts the one file BMAD does name first. Pinned in `test/domain/document.test.ts` from both sides — the fixture's names are handed over unsorted and again reversed, and the expected list is compared whole, so a mutation that drops the index from first position or reorders the rest fails.
  findings_already_measured: Only markdown children are parts, matched by the same `.md` rule `identity.ts` uses for its structural read. Two knock-ons, both stated in `src/domain/document.ts` rather than left to be discovered: a run folder's `.memlog.md` **is** a part of the sharded *reading* of an ambiguous directory (one more reason the ambiguity is carried rather than resolved), and a *directory* named `part.md` would be counted as a part, because the listing the pass hands over is names only — asking the filesystem for kinds would be the second enumeration `src/cli/inventory.ts`'s header rules out.
  triggers: Epic 2, Stories 2.9–2.11 and FR-77 — the first work that adopts real shard boundaries as pages, at which point the order becomes user-visible and this choice should be replaced by a measured one or a stated design.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-handle-whole-and-sharded-documents-alike.md`
  summary: LIMIT, stated — a directory of markdown with **no `index.md`** and no run-folder signal is not claimed as a document at all, so a shard set BMAD wrote without an index is invisible as a document.
  evidence: The frozen I/O matrix's own row, implemented as it is written and recorded here because it is a real gap rather than a rule with an escape hatch. `review-edge-cases.md:290` measured the discriminator problem exactly: an `index.md` "may be absent from a shard set or present in a run folder", so it is neither necessary nor sufficient, and with the index absent there is no second signal in the filesystem to claim a composition from. Inventing one — "several markdown files in a directory means a sharded document" — would report every run folder and every `reviews/` subfolder as a document. The narrower consequence is recorded with it: composition **mirrors the recorded verdict** (AD-4), so an artifact no precedence level identified has no recorded shape and therefore no composition either; Story 1.9 lists it as present-but-uninterpreted. Asserted in `test/domain/document.test.ts` in both directions.
  triggers: The first project found in the wild with an index-less shard set, or any story that gains a signal a shard set can be recognized by — a sibling manifest, a configured shard path, a `bmad-build` convention that writes one.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-handle-whole-and-sharded-documents-alike.md`
  summary: LIMIT, stated — **there is no sharded document anywhere in this repository**, so every sharded-shape assertion in this story is against a fixture rather than against a measured BMAD output.
  evidence: Measured 2026-09-02: `find _bmad-output -name index.md` returns nothing, and `find _bmad-output -type l` returns nothing either, so neither the sharded shape nor the alias-suppression row has a real example here. Stated in the spec as a limit rather than an oversight, and kept honest from the other side by a test: `test/cli/inventory.test.ts` asserts over the project's own tree that nothing composes to a `sharded-document` and that no suppression occurs, so the day a real one lands the suite says so instead of quietly gaining coverage nobody checked. Every fixture in `test/domain/document.test.ts` builds its verdict by calling `identify` rather than writing one out, so no shape is exercised that the authority could not actually produce — which is the closest a fixture gets to being measured.
  triggers: The first sharded BMAD document available to test against — from a real project, or from BMAD's own sharding once a version writes one — at which point the fixtures should be replaced or joined by it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-handle-whole-and-sharded-documents-alike.md`
  summary: OPEN QUESTION, unresolved by decision — whether a sharded document is **one artifact view or many** is still unanswered, and the model deliberately does not decide it.
  evidence: `review-state-coverage.md:117` lists it among what "a developer must invent", together with whether the contents rail lists headings or shard files and whether the difference is disclosed. Nothing in this story needs the answer: `Composition` carries one identity and an ordered list of part paths, which supports either reading — one view paging through the parts, or one view per part — and picking now would be a UX decision taken in a domain module. `EXPERIENCE.md:166` says the reader "looks identical either way" and that the rail "does not disclose which it was", which constrains the *presentation* and still does not answer the view-count question.
  triggers: Stories 2.9–2.11, the first work that renders a sharded document and therefore has to choose. It should also decide what a permalink into a shard resolves to, which is AD-18's half of the same question.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-handle-whole-and-sharded-documents-alike.md`
  summary: COST of this story's own fix — a part path can name a spelling that has **no entry of its own**, because restoring a suppressed alias puts a name back into a listing while the artifact at that identity is reported under the other spelling.
  evidence: Recorded 2026-09-02 in the review round, where it had been stated in `src/cli/inventory.ts`'s header and nowhere a later story would look. The shape: `dirA/index.md` is a symlink to `dirB/real.md`, the walk reports `dirB/real.md` and suppresses `dirA/index.md`, and the pass restores the name so `dirA` still reads as possibly sharded — so `dirA`'s sharded reading has a part `dirA/index.md` that appears in no `InventoryEntry`. Two things bound it, both asserted: an alias beside its *own* target in the same directory is excluded from parts (one file is one part, so nothing renders twice), and every restore decision is on `Inventory.aliases` with `restored` and a reason. What is left is the cross-directory case, where the part path is the honest name of what that directory holds and is still a path no entry keys. The alternative was not restoring the name at all, which is the defect this story exists to close.
  triggers: Story 1.9, which lists what could not be read and will have to decide whether such a part is one of those; and Epic 2, the first work that opens a part by path and may find it has no snapshot entry to render a header from. Either may reasonably decide the pass should emit an entry for a restored alias after all — the decision recorded here is "an alias is not an entry", taken in this story for the walk's cycle set, not for them.
  findings_already_measured: The Story 1.9 half fired on 2026-09-02 and was answered **no**: a part path with no entry of its own is not one of the things this story lists as unreadable. Readability is recorded per `InventoryEntry` — one signal per artifact, as AD-8 has it — and an alias is not an entry, so it has no readability signal, and inventing an `unchecked` row for a path the pass never walked would put a name in the inventory by the side door that 1.8's restore deliberately kept out of it. Where that fact does live is unchanged and is a better place for it: `Inventory.aliases` carries the spelling, where the artifact is reported instead, whether the name was restored, and why. So the Epic 2 half of this trigger stands exactly as written, and the decision "an alias is not an entry" now holds for two stories rather than one.

## Deferred from: implementation of spec-1-9-show-what-could-not-be-read-or-recognized (2026-09-02)

Five entries, none of them a review finding in the usual sense — review round 1
of this story produced twenty findings and nineteen were applied in the round
itself. The first two are limits the story's own spec required be **recorded
rather than silently narrowed**: one accepted deliberately in the frozen
intent, one a rule this story is listed as satisfying and could not bind. The
third is a gap in a normative document that the first surface to render this
work will hit. The fourth is what is left of the untyped-`reason` question
closed above, kept open so the closure is not the last word on it. The fifth is
the one round-1 finding not fixed in the round, because fixing it would mean
deleting vocabulary AD-8 requires.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-show-what-could-not-be-read-or-recognized.md`
  summary: LIMIT, accepted deliberately — a file that cannot be decoded as text is reported `unreadable` only where a precedence level actually read it. **Under an artifact root, where level 1 resolves without reading, it is reported `unchecked`.**
  evidence: Measured at this story's baseline and again after it: a file of invalid UTF-8 bytes named `prd.md` under `_bmad-output/planning-artifacts/prds/` came back `identified`, `family: prd`, `confidence: certain`, with `attempted: [location:resolved]` and no reason anywhere — corrupt data presented as valid, which NFR-3 forbids by name. That half is fixed: the entry now carries `readability: { state: 'unchecked', reason: 'no precedence level needed this content, so nothing read it' }`, so nothing claims the content is fine. What is *not* fixed is the story's own clause "a file that cannot be decoded as text is reported as unreadable", which holds only for files some level read. The alternative is reading eagerly to establish readability for files no level needs, and that trade was taken the other way deliberately and for a measured reason recorded at `src/cli/inventory.ts`'s header: read eagerly, the pass pulled up to `maxEntries` × `MAX_READ_BYTES` through a synchronous decoder and discarded almost all of it, and every PNG in a mockups folder came back as "not valid UTF-8". Pinned from both sides rather than left as prose: `test/cli/inventory.test.ts` asserts the file under a root is never opened *and* that its state is `unchecked` and not `present`, and asserts the same property over this repository's own tree — every entry level 1 resolved is `unchecked`, and the entries that are `present` are the ones a later level had to read.
  triggers: Epic 2, the first work that reads a document's content for its own sake — at which point the readability of a file under an artifact root stops being unknown and this limit stops existing. Story 1.12 sooner, if `Not checked` over an artifact root proves misleading on the rendered surface; that is a decision about copy, not about this trade.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-show-what-could-not-be-read-or-recognized.md`
  summary: LIMIT, stated — `epics.md:365` lists AD-13 among what this story satisfies, and **AD-13 has no code to bind here**: its subject is BMAD's status vocabularies, which live in YAML *comments* nothing in this tool reads yet.
  evidence: Measured 2026-09-02 while implementing. AD-13's rule is "any parser reading a source BMAD defines by convention rather than contract — status vocabularies live in YAML *comments* — validates that it extracted something, and fails visibly when it did not". Nothing in `src/` reads `sprint-status-template.yaml` or any YAML at all: the first YAML parser arrives with FR-10/FR-23 and brings the first bundled library with it. What this story could honour of the rule, it honours, and it is asserted rather than asserted-to: the nearest convention-defined parser is `src/domain/frontmatter.ts`, which separates *absent* from *undecodable* and reports an unclosed block as unavailable with its reason instead of returning an empty success — `test/cli/inventory.test.ts`'s row "a frontmatter block that is never closed is unavailable, not an empty success" holds that through the real pass, including that the block's `type: prd` is **not** taken as a declaration. Recorded because "satisfies AD-13" in the epic would otherwise read as a claim this story made good on its actual subject.
  triggers: Story 2.8 (FR-23, NFR-6) — the first parser that extracts a status vocabulary from YAML comments, and the first place AD-13 has something to fail loudly about.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-show-what-could-not-be-read-or-recognized.md`
  summary: GAP in a normative document — `EXPERIENCE.md`'s load-bearing string index has a row for the unidentified case and the four signal states, and **no row for FR-12's present-but-uninterpreted state**, so the first surface to render it has no authoritative copy to use.
  evidence: Recorded 2026-09-02, as the half of the FR-12/FR-69 collision that settling the vocabulary in code does not answer. The index carries `Artifact unidentified | Not identified. Tried: config path, frontmatter, structure, filename.` and the four states (`Present`, `Not found`, `Unreadable`, `Not checked`), which this story reads out of the document by test rather than copying. It carries nothing for a *recognized* artifact whose shape nothing can interpret, and `EXPERIENCE.md:170` merges the two terms into one sentence — the collision itself. The model is not blocked: the state is recorded on every inventory entry and its definition is data. What is missing is the string, and it is deliberately not invented here, because the string index is normative and every entry in it is used verbatim — page copy authored in a domain module is exactly the drift the index exists to prevent.
  triggers: Story 1.12, which renders the inventory and is the first surface where the state is shown. It must either add a row to the string index or state which existing row it uses and why.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-show-what-could-not-be-read-or-recognized.md`
  summary: RESIDUE of a question closed in this story — `src/adapters/fs/list.ts`'s `Listing` and `ConfinedReader`'s `Entry` still carry an untyped `reason: string` and no typed state, by decision rather than by omission.
  evidence: Recorded 2026-09-02 so that closing the untyped-`reason` entry above is not the last word on the two values it names. The shape they would adopt now exists (`src/domain/signal.ts`), and `ConfinedReader.readText` adopted it in this story. Neither of these two is consumed as a *state* by anything: the suggestion scan discards `Listing.reason` and reports paths and counts, and `Entry`'s only consumer, `resolveLocation`, maps every kind into `Refusal` — this project's own closed cause-plus-raw-text vocabulary — so a typed state here would be re-mapped into another typed state at the one call site that reads it. Note `Entry` already spells two of AD-8's four names (`absent`, `unreadable`) as *kinds*, which is the drift a shared vocabulary is meant to stop and is why this stays on the record rather than being declared a non-issue.
  triggers: The first consumer that branches on either value's state rather than on its kind, or the first surface that shows either `reason` to a user — which the correction on the closed entry establishes has never yet happened, for `Listing` at least.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-show-what-could-not-be-read-or-recognized.md`
  summary: (low) Seven of Story 1.9's new exports have **no consumer in `src/`** — `SIGNAL_STATES`, `SIGNAL_LABELS`, `READ_STAGES`, `SignalState`, `INTERPRETATION_STATES`, `INTERPRETATION_DEFINITIONS` (as a value), `definitionOf` and `requirementOf` — so they are held in place by tests, which is the shape this project's own closing notes call out.
  evidence: Raised in review round 1 on 2026-09-02 and **not fixed in the round, by decision**, which is why it is here rather than in the diff. Deleting them is the obvious answer and it is the wrong one: AD-8 says its four states are "the single vocabulary used by the model, the conventions table, and the UI", so `SIGNAL_LABELS` living in the domain — read out of `EXPERIENCE.md`'s string index by test, never restated — is the arrangement the spine asks for, and the same argument covers the states and the definitions. What is true is that nothing in `src/` reads them yet, for the same reason nothing renders anything in this story: `src/render/page.ts` is still the placeholder surface. The precedent this follows is the pass's own asserted-empty importer set, recorded under Story 1.7 rather than resolved by wiring a surface nobody had designed. What each one does have is a test that would fail if it drifted from the normative document it mirrors, which is the difference between unconsumed and unchecked.
  triggers: Story 1.12, the first surface to render an inventory. If it renders the four signal states or FR-12's state **without** reading these, that is a second copy of a normative vocabulary and a finding about 1.12 — and this entry is what says so in advance.

## Deferred from: implementation of spec-1-10-get-bmad-s-own-irregularities-right (2026-09-02)

Seven entries. Five were recorded with the implementation; the last two came
out of the review round on 2026-09-02, along with two corrections left in place
beside the text they correct. None is a deferred *defect* — every finding the
round raised about the code was fixed in the round. The first is a limit this
story's own acceptance names and **does not deliver**, recorded rather than
quietly dropped. The second and third are places where this tool's tables are wider or
narrower than what any document specifies, adopted deliberately here rather
than waiting for FR-10. The fourth is a gap in a normative document that the
first surface to render this work will hit — the same shape as Story 1.9's
FR-12 entry, and a sibling of it. The fifth is a family the vocabulary declined
to add.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: LIMIT, accepted deliberately and stated in the frozen intent — FR-71's check as `epics.md:379` words it ("a folder containing two same-day runs of one family reports both") is **undeliverable from a folder's contents and is not delivered**. What ships is FR-71's negative clause plus the disclosure that a collision is possible.
  evidence: Measured 2026-09-02. The signal that would tell one run from two does not exist in a BMAD run folder, and `src/domain/runs.ts`'s `RUN_COUNT_BASIS` records the four candidates item by item: `memlog.py`'s `init` errors on a second run so both runs append to one log rather than starting a second; document frontmatter dates are day-resolution and are overwritten in place; no file a run writes is numbered or otherwise per-run; and filesystem mtime is FR-14's own tier 6, "actively misleading after a clone, checkout or pull". No fixture can be built that a reader could distinguish from a single run, so a test asserting "reports both" would be asserting against a fabricated tree. What is delivered instead is typed and pinned: every run folder carries `runCount: 'unknowable'`, `RunCount` is a single-value union so a claimed number fails the typecheck, and `test/domain/runs.test.ts` asserts the fact object's key set exactly so an added count fails the suite. Approximating the count from mtimes is on this story's Never list and was not done.
  triggers: Epic 3, where git lands (FR-62 and FR-14 tiers 1 and 2). Commit timestamps are the only per-event chronology BMAD does not provide, so two same-day runs of one family become distinguishable there and nowhere earlier.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: FRAMING, deliberate and wider than the requirement — FR-71 says "Four of seven run-folder patterns" collide, and `src/domain/runs.ts` records **seven of seven**, split by whether the reuse is deliberate.
  evidence: FR-71's literal wording is true of its four: `brief-|prd-|architecture-|ux-{project_name}-{date}` have both components constant within a day. It understates the surface, because the other three reuse their names too and BMAD intends them to — a spec folder is reopened by slug (FR-72 says so in the same breath), a forge folder by bare slug, a research folder by topic. So the fact is two axes rather than one: `canCollide`, true for all seven, and `reuse`, which separates the three BMAD offers to resume from the four where a same-day rerun is an accident. The dangerous case is the accidental four, and it is the opposite of what a naive reading suggests: their resume offer is conditional on the existing document not being final, so a same-day re-create over a *finalized* document overwrites with no prompt. Recorded because a reader comparing the table with FR-71 will find seven where the requirement says four, and that difference is a decision rather than a transcription error — `test/domain/runs.test.ts` asserts both sides of the split and asserts that FR-71's own sentence still says "Four of seven".
  revisit: Whenever FR-71 is next edited. If the PRD adopts the seven-of-seven framing, the quoted definition in `RUN_FACT_DEFINITIONS` moves with it and the test that reads it out of `prd.md` will say so.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: FRAGILITY, adopted deliberately — the seven run-folder patterns, the six research types and the `{kind}-{slug}` prefixes are **measured against BMAD v6.11.0 and specified nowhere**. `RESEARCH_TYPES` is open in BMAD and closed here, so a user-added custom research type does not match any pattern.
  evidence: The patterns come from each skill's `customize.toml` `run_folder_pattern`; `bmad-deep-recon` supplies the six shipped type packs and documents user-added custom types. A dated folder whose prefix is not one of the six has no pattern match at all: it is identified by *location* if it sits under `_bmad-output/planning-artifacts/research`, which is what level 1 is for, and is unidentified if it sits anywhere else. That is the same class of fragility as the artifact roots themselves, recorded under Story 1.7, and it has the same fix. This story adopted it rather than waiting, because its trigger — FR-10, reading the project's own `_bmad/bmm/config.yaml` — is deferred indefinitely, and widening `RESEARCH_TYPES` beyond the six measured is on this story's Ask First list.
  triggers: FR-10, whenever it lands. Sooner if a real project turns up with a custom research type outside the research root, which is the one case location cannot rescue.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: GAP in a normative document — `EXPERIENCE.md`'s load-bearing string index carries **one** row for this state (`Run folder may hold several runs | This folder may contain more than one run.`) and no wording for the two distinctions this story records: deliberate versus accidental reuse, and a date signal that is *absent* rather than zero.
  evidence: Recorded 2026-09-02. The story's frozen intent says the index "has no row for it"; measured against the document, it has one, and that one covers only the undifferentiated FR-71 disclosure. What it does not carry is copy for the case that matters most — an accidental collision over a finalized document, which overwrites with no prompt — or for FR-72's dateless pair, whose nearest neighbour is the evidence-tier label `None` and that is Epic 3's tier vocabulary rather than a run-folder fact. The model is not blocked: both facts are recorded on every inventory entry and their definitions are FR-71's and FR-72's own sentences, read out of `prd.md` by test. What is missing is the string, and it is deliberately not invented in a domain module, because the string index is normative and every entry in it is used verbatim.
  correction: 2026-09-02, review round. Two additions. First, the index's coverage is slightly better than this entry first said: besides `:90`'s row there is an edge-condition paragraph at `:174` ("**Run folder holding several same-day runs**"), which is the same undifferentiated disclosure in prose. Second, and new to this story: adding `review` to the family vocabulary makes `:100`'s templated `No <family> runs in this project.` **false for a family that is never a run** — a review has no run folder, so `No review runs in this project.` is a sentence the template can now produce and no project can make true. Story 1.12 must either exclude non-run families from that template or add a row for them.
  triggers: Story 1.12, which renders the inventory and is the first surface where these facts are shown. It must either add rows to the string index or state which existing row it uses and why — the same instruction Story 1.9's FR-12 entry above carries, and the two should be answered together.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: DECLINED, and recorded so it is a decision rather than an oversight — `review` became a family in this story; **`reconcile` did not**, so `reconcile-{slug}.md` resolves to no family at any level.
  evidence: FR-49 (`prd.md:178`) names **three** slug positions and this story added a family for one of them, on the human's decision that a review is a different artifact from the thing it reviews. (Corrected 2026-09-02, review round: this entry first said four, repeating a misattribution the spec's Code Map carried — the fourth position, `{research_type}-{topic_slug}-{date}`, is `bmad-source-shapes.md`'s, not FR-49's.) A reconciliation is the same kind of thing by the same argument, and it was not added because nothing renders it and no measured project in reach contains one — adding a family with no surface is the shape this project's own closing notes call out. What ships instead is the honest middle: `KIND_PREFIXES` records `reconcile-` with **no** family, so the prefix is recognized and stops the slug being read, and level 1 *declines* the enclosing root's family rather than taking it. The consequence is pinned from both sides in `test/domain/identity.test.ts`: `reconcile-prd.md` is unidentified inside a `prds/` root as well as outside one, and is never read as a `prd`. So a reconciliation is reported as present-but-uninterpreted (FR-12) rather than as its own kind.
  triggers: Story 1.12 or Epic 2, whichever first has a reconciliation to show — FR-21 ("reconciliation as gaps") is Epic 2's and is the requirement that will want the family. Also the third `Confidence` value, which this story's Ask First list defers to the surface that renders it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: LIMIT, structural and unfixable by a name rule — the **forge bare `{slug}`** is a fifth slug position that neither the prefix table nor the pattern table can cover, so a forge run called `prd-redesign` outside `_bmad-output/forge` still reads `prd` from the hint table.
  evidence: Raised in the review round on 2026-09-02, after the coverage test was found to assert the other four positions and pass silently over this one. A bare slug has no prefix, and a rule broad enough to match it would match every directory — which is why `RUN_FOLDER_PATTERNS` has no forge row and why `identity.ts` already says a forge run is recognizable by location alone. Inside `_bmad-output/forge` level 1 answers and the name is never consulted, so the reading is only wrong for a forge folder that has been moved or copied out of its root. Not left as prose: `test/domain/identity.test.ts` now pins the known-bad reading itself — `prd` at `filename`, `likely` — beside the correct in-root reading, so a change to either is deliberate.
  triggers: FR-10, which would at least make the forge root's location configurable rather than measured; and Epic 3's git signals, which are the only evidence that could identify a moved run folder by anything other than its name.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-get-bmad-s-own-irregularities-right.md`
  summary: (low) Seven of this story's new exports have **no consumer in `src/`** — `RUN_PATTERNS`, `RUN_COUNT`, `RUN_COUNT_BASIS`, `RUN_FACT_AXES`, `RUN_FACT_DEFINITIONS`, `Reuse` and `DateSignal` — so they are held in place by tests, exactly as Story 1.9's entry above records for `SIGNAL_LABELS` and the interpretation definitions.
  evidence: Raised in the review round on 2026-09-02 for not being recorded, which is the finding rather than the exports themselves. The precedent directly above is followed rather than re-argued: the vocabulary and the measured facts belong in the domain, each is pinned against the document it mirrors — `RUN_FACT_DEFINITIONS` against `prd.md`'s FR-71 and FR-72, `RUN_PATTERNS`' date signal against `identity.ts`'s own `dated` flags, `KIND_PREFIXES.slugNames` against `bmad-source-shapes.md`'s table — and nothing in `src/` reads them yet for the same reason nothing renders anything: `src/render/page.ts` is still the placeholder surface. `runFactsOf` and `runFolderFamiliesOf` *are* consumed, by the pass. What each unconsumed export has is a test that fails if it drifts from its source, which is the difference between unconsumed and unchecked.
  triggers: Story 1.12, the first surface to render an inventory. If it renders a collision disclosure or a date-signal state **without** reading these, that is a second copy of a measured fact and a finding about 1.12 — and this entry is what says so in advance.

## Resolved and closed

Kept verbatim rather than deleted — this file exists so nothing is lost, and a
resolved finding is evidence about what this project gets wrong. Moved here on
2026-09-02 so the list above is only what is actually open: it had 43 entries,
of which 15 were already closed, superseded, or triple-recorded.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-identify-what-each-artifact-is.md`
  summary: (medium) A child the walk suppressed as a **symlink alias** vanishes from the listing the identification pass hands the domain, with `complete: true` and no truncation — so an `index.md` that is a second spelling of an already-seen real path does not make its directory read as a sharded document.
  evidence: Found in the review round of 2026-09-02 and **recorded rather than fixed, deliberately**. `src/adapters/fs/walk.ts` drops a child whose resolved real path it has already seen, and its header states why at length: a resolved path is the identity, so emitting a second entry for one would put into the inventory the exact duplicate the cycle set exists to prevent. That is the right call for the walk. The consequence one layer up is that "the children are what the directory holds" has a hole the pass cannot see: the walk reports nothing at all for a suppressed alias, so `childNamesByParent` cannot know a name was dropped, and `complete` and `truncations` both say the pass was clean. Two candidate fixes, neither cheap: have the walk report suppressions as a separate list (a new field on `WalkResult`, and a decision about whether an alias is an entry), or have the pass re-enumerate each directory through `childrenOf` (a second probe of the whole tree, which the pass avoids on purpose). Both are more than a review round. What the round did do is state the hole in `src/cli/inventory.ts`'s own header beside the skip-policy hole it sits next to, so the next reader finds it before the next reviewer does.
  triggers: Story 1.8, which owns whole-versus-sharded documents and is the story that actually turns on whether an `index.md` was seen. If a project in the wild is found symlinking shards, it becomes a defect rather than a hole.
  closed: RESOLVED 2026-09-02 by Story 1.8, by the first of the two candidate fixes above rather than the second. `src/adapters/fs/walk.ts` now reports every suppression in `WalkResult.suppressed` — naming both spellings, the resolved path they share, and the kind — capped at `MAX_RECORDED_SUPPRESSIONS` with the overflow counted, because a suppressed child never becomes an entry and so was never bounded by `maxEntries`. A suppression also counts against `complete`: the walk shortened a directory's listing, and that is the narrow claim rather than a claim that an artifact went missing. The decision the entry said was owed — *whether an alias is an entry* — is answered **no**: it stays out of `entries`, so one resolved identity is still reported once. `src/cli/inventory.ts` then **restores** the name to its parent's listing, which is what actually closes the hole: an aliased `index.md` now makes its directory read as FR-73 ambiguous rather than as an `identified`, `certain` run folder. Two facts make the restore safe and both are asserted — a suppressed child was resolved and confined like any other, so this does not admit the dangling name the `present` filter excludes, and the artifact at that identity is in `entries` under its first spelling, so nothing is duplicated. What it costs is that a part path can name a spelling with no entry of its own, which is the honest report of a directory holding two names for one file. Covered in `test/adapters/walk.test.ts` (reported, capped, not a truncation, not complete) and `test/cli/inventory.test.ts` (restored, ambiguous, parts naming the alias, one entry per identity).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: RESOLVED 2026-09-01 by Story 1.4 — `--help`, `-h` and `--version` are implemented and exit 0, the help text is reachable on request rather than only by error, and FR-6's flag is named `--no-open`. The deferral test in test/server.test.ts was inverted to assert the new behaviour. Original: No `--help`, `-h` or `--version`; all three exit 2 as unknown arguments and the USAGE string is reachable only by error.
  evidence: Reviewers confirmed `parseArgs` runs strict with an empty options map. A published CLI with no --version is hard to support. FR-6's suppression flag is also still unnamed in the PRD.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: RESOLVED 2026-09-01 by Story 1.4 — `--port <n>` is exposed, validated as a plain decimal integer in range, passed to the server, and a fallback to an OS-assigned port is now reported rather than silent. This also makes the port-80 `Host` allowance and the bind-retry path reachable outside tests, which was the reason for exposing it. Original: (medium) Expose `--port <n>` on the CLI
  evidence: From the 2026-09-01 code review, located and verified there. Full text: Expose `--port <n>` on the CLI [src/cli/index.ts:54-89, 161] — resolves the decision above. Add a `port` option to the `parseArgs` call (strict, so an unknown flag still exits 2), validate it as an integer in `0..65535` and exit 2 naming the value and the range on failure, pass it into `start(...)`,

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-build-the-components-every-surface-reuses.md`
  summary: RESOLVED 2026-09-02 by Story 1.5 — the CLI now recognizes its target before binding: existence, directory-ness, both markers and readability are all checked, each with a distinguishable message, and the root is canonicalized through the platform's own resolver so a symlinked or differently-cased target yields one identity. Original: (medium) `assertProjectRoot` checks that the root is a non-empty absolute path, but not that it exists, is a directory, or is canonical — so a non-existent path renders as a project, and a symlinked invocation displays a path that differs from the canonical root later stories key artifacts by.
  evidence: From the Story 1.3 review, 2026-09-01, verified against src/render/chrome.ts. Existence and canonicalization are Story 1.5's, which resolves the root by the presence of `_bmad` and `_bmad-output` and canonicalizes at the filesystem adapter. Deferred rather than half-implemented: a partial existence check in the render layer would need `node:fs`, which AD-1 forbids there, and would duplicate the resolution Story 1.5 owns.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-establish-the-visual-foundation.md`
  summary: RESOLVED 2026-09-01 — user chose the relative ladder; DESIGN.md's Elevation & Depth section rewritten to distinguish peer levels from nested surfaces, hover moved to the outline, and the tonal-separation claims put under test. (medium) DESIGN.md contradicts itself on the elevation ladder — its Elevation & Depth table states that `{colors.surface-container}` and `{colors.surface-container-highest}` "are not used at rest in v1", but `components.core-artifact-card` uses `surface-container-highest` as its background and `surface-container` as its absent background, both at rest.
  evidence: From the Story 1.2 review, 2026-09-01, verified against DESIGN.md:267 and DESIGN.md:360-368. Not resolved in the build story because DESIGN.md is the normative side and this is an internal contradiction within it rather than code drift — resolving it is a UX decision about whether the ladder has three levels or five. Blocking for Story 1.3, which builds the core-artifact card and cannot pick a surface token from an ambiguous ladder.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: A nonexistent or non-directory target path is accepted without complaint; the tool serves a dashboard over nothing.
  evidence: Confirmed by a reviewer. Deliberate for this story — the spec assigns project recognition to Story 1.5 (FR-7) — but it must not be lost, because until 1.5 lands a typo'd path looks like a working tool.
  closed: RESOLVED 2026-09-02 by Story 1.5. Superseded and simply never marked — recognition now happens in the composition root before the socket binds. It was still sitting in the open list a day after it closed.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) Vacuous assertion
  evidence: From the 2026-09-01 code review, located and verified there. Full text: Vacuous assertion [test/discovery/nested.test.ts:82] — `assert.ok(fromTestDir.split(sep).length >= 1)` can never fail; `String.prototype.split` never returns an empty array. The two assertions above it do the real work.
  closed: RESOLVED 2026-09-02. `test/discovery/nested.test.ts` now asserts the depth on the *file* path with `>= 2`, which fails if the file moves up into `test/`. Mutation-checked: tightening it to `>= 3` fails the test, so it is live.
  mutations: Re-run 2026-09-02, not inherited. Tightened `test/discovery/nested.test.ts:89` to `>= 3`: `this test file is itself in a subdirectory of test/` failed, 16 pass / 1 fail in that file. Restored. The claim holds.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) `parsePattern` accepts a padded override and passes it through untrimmed
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `parsePattern` accepts a padded override and passes it through untrimmed [scripts/test-run-policy.ts:67-73] — `parseFloor` trims, this does not, so `' test/**/*.test.ts '` is accepted, matches nothing, and is reported as "discovery is collecting less than the whole suite" rather than as a bad patter
  closed: RESOLVED 2026-09-02. `parsePattern` trims like `parseFloor`, and a pattern that is only padding is still refused. Mutation-checked: removing the trim fails the new test in `test/tooling/run-policy.test.ts`.
  mutations: Re-run 2026-09-02, not inherited. Replaced `raw.trim()` with `raw` in `parsePattern`: `a padded pattern override is trimmed rather than passed through` failed, 12 pass / 1 fail. Restored. The claim holds.
  correction: 2026-09-02, second review round. The `closed:` line above credits the fix with one thing it did not do: "a pattern that is only padding is still refused" was **pre-existing** behaviour. The `raw.trim() === ''` branch is there in `cef8da8`, unchanged — verified by reading it. The fix was the trim on the *accepted* path only. The new test's second assertion and this note both described the old branch as if it were new. Separately, that branch's message said the variable "is set but empty", which is inaccurate for `'   '` — set, and not empty; the wording is now "an empty or whitespace-only value".

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) Stale `.mjs` references, one of them load-bearing
  evidence: From the 2026-09-01 code review, located and verified there. Full text: Stale `.mjs` references, one of them load-bearing [test/architecture.test.ts:38, 321; test/discovery/nested.test.ts:10] — the runner is `scripts/run-tests.ts`. Consequentially, `test/architecture.test.ts:321` scopes the stale-`.js` guard to `src/` on the premise that "`scripts/` is plain `.mjs` tool
  closed: RESOLVED 2026-09-02. Two of the three named sites had already gone (`test/architecture.test.ts:38, 321`); the third, a header comment in `test/discovery/nested.test.ts` naming `scripts/run-tests.mjs`, is corrected. The load-bearing consequence the finding warned about — the stale-`.js` guard scoped to `src/` on a false premise — no longer exists in the code.
  mutations: Re-checked 2026-09-02, and **the last sentence above is wrong**. No mutation applies — the fix was a comment — so the claim was read against the code instead. The three `.mjs` references are indeed gone (the only survivors are extension lists in `test/support/gate.ts:62` and the guard's own pattern, both correct). But the guard at `test/architecture.test.ts:134-138` is *still* scoped to `src/`: only the false premise in its comment was removed, not the scoping the premise justified. Re-opened as its own entry in the list above.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) `bmad-dash ""` silently targets the working directory
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `bmad-dash ""` silently targets the working directory [src/cli/index.ts:83] — `resolve(cwd, positionals[0] ?? '.')` turns `''` into `cwd`, so an empty argument looks like a successful default rather than misuse. Distinct from the nonexistent-path case already deferred to Story 1.5; this one is an ar
  closed: RESOLVED 2026-09-02. An empty positional is now a usage error naming the fix. Deliberately only the empty string: `" "` names a directory called one space, which is legal and is not the working directory, so refusing it would refuse something a caller could have meant. Mutation-checked: disabling the guard fails the new test in `test/cli/flags.test.ts`. Independently re-found by the Story 1.5 review, which is some evidence this list is not being read.
  mutations: Re-run 2026-09-02, not inherited. Forced the `given === ''` branch in `src/cli/index.ts:292` to never be taken: `an empty path argument is refused, not treated as the default` failed, 17 pass / 1 fail. Restored. The claim holds.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) `stopWith`'s 8s timer is never cleared, though `deadline()` exists to do exactly that
  evidence: From the 2026-09-01 code review, located and verified there. Full text: `stopWith`'s 8s timer is never cleared, though `deadline()` exists to do exactly that [test/server.test.ts:271-278] — `t.unref()` means it holds nothing, so the consequence is a stray `SIGKILL` attempt on an already-dead child rather than a leak. Use `deadline()` (`test/server.test.ts:357`) or clear
  closed: RESOLVED 2026-09-02. Cleared in a `finally` after the race. **No test**, and that is not an oversight: the consequence was a stray `SIGKILL` attempt at an already-dead child rather than an observable behaviour, exactly as the finding said, so there is nothing an assertion could catch. Recorded rather than claimed as verified.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) The `files`-whitelist test's comment claims an existence check it does not perform
  evidence: From the 2026-09-01 code review, located and verified there. Full text: The `files`-whitelist test's comment claims an existence check it does not perform [test/cli-entry.test.ts:164-170] — the comment reads "Every whitelisted entry must be something that actually exists, so a stale entry cannot sit in the manifest looking like a shipped directory", but the assertion is
  closed: RESOLVED 2026-09-02. The assertion now does what the comment claims, using the `access` already imported in that file. Mutation-checked: adding a non-existent entry to `files` in package.json fails the test.
  mutations: Re-run 2026-09-02, not inherited. Added `"no-such-directory"` to `package.json`'s `files`: `the files whitelist publishes the directory holding the bin` failed, 20 pass / 1 fail in `test/cli-entry.test.ts`. Restored. The claim holds.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: (low) Two *Suggested Review Order* pointers no longer land on the code they describe — this spec says `index.ts:167` is "Readiness is announced last" (the `stdout` call is `src/cli/index.ts:175`) and `test-run-policy.ts:75` is "Takes the last summary, not the first" (a blank line; `readTotal` is `scripts/test-run-policy.ts:84`). Spec-side drift; the other eleven pointers are accurate.
  evidence: From the 2026-09-01 code review, located and verified there. Full text: Two *Suggested Review Order* pointers no longer land on the code they describe — this spec says `index.ts:167` is "Readiness is announced last" (the `stdout` call is `src/cli/index.ts:175`) and `test-run-policy.ts:75` is "Takes the last summary, not the first" (a blank line; `readTotal` is `scripts/
  closed: CLOSED BY DECISION 2026-09-02, not fixed — raise it again if you disagree. The pointers sit in a *completed* story's spec and were accurate for the code as it stood at that review. Re-pointing them at today's line numbers would make a Story 1.1 document describe code written in 1.4 and 1.5, which is worse than leaving them stale: the spec records a review that happened, it is not a live index.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: Repository hygiene absent — no .gitignore, README, LICENSE, repository or author fields, and no CI workflow running typecheck or tests.
  evidence: Confirmed by two reviewers. Partly downstream of the deferred `git init`; the license needs a human decision. CI matters because nothing automated currently runs `tsc --noEmit`.
  closed: SUPERSEDED 2026-09-02, not resolved — the work is still open, recorded once instead of three times. See "Repository hygiene, consolidated" in the open list above, which states what is actually still missing (README, `keywords`, CI, lint/format, coverage) and what has since landed (`.gitignore`, `LICENSE`, `license`, `author`, `repository`, lockfile).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: No README, and no `license`, `repository`, `author` or `keywords` in package.json, for a package whose whole interface is one npx command.
  evidence: Two reviewers noted the npm page would be blank. License needs a human decision; the rest are mechanical but out of scope for a story about serving a page.
  closed: SUPERSEDED 2026-09-02, not resolved — the work is still open, recorded once instead of three times. See "Repository hygiene, consolidated" in the open list above, which states what is actually still missing (README, `keywords`, CI, lint/format, coverage) and what has since landed (`.gitignore`, `LICENSE`, `license`, `author`, `repository`, lockfile).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-run-the-command-and-reach-a-served-page.md`
  summary: No CI workflow, no lockfile committed, no lint or format config, and no coverage instrumentation despite a suite whose entire thesis is coverage.
  evidence: Nothing automated runs `tsc --noEmit` outside a contributor's own machine. Coverage matters here specifically because three review rounds each found gaps that a green suite could not see.
  closed: SUPERSEDED 2026-09-02, not resolved — the work is still open, recorded once instead of three times. See "Repository hygiene, consolidated" in the open list above, which states what is actually still missing (README, `keywords`, CI, lint/format, coverage) and what has since landed (`.gitignore`, `LICENSE`, `license`, `author`, `repository`, lockfile).

- source_spec: none
  summary: (high) Build the safe artifact-tree walk — cycle-detecting directory traversal in which symlinks are resolved before confinement is checked and before identity is keyed, a link inside the tree pointing outside it cannot pass confinement, the same file reachable two ways does not appear as two artifacts, and a dangling link is reported rather than crashing the listing. Should land as its own story immediately before Story 1.7.
  evidence: Split from Story 1.5 at the step-1 multi-goal check, 2026-09-02, by user decision. Story 1.5's second paragraph specifies these as properties of "the walk" and "the listing", but no story in Epic 1 builds an enumerator — 1.7 identifies artifacts and 1.9 refers to "crashing the walk", so both assume one already exists. Carved out rather than folded into 1.5 because the walk's failure modes (cycles, dangling links, permission errors, case-insensitive collisions) are a different kind of risk from config parsing and root resolution, and pairing them in one review round is the shape that produced three consecutive bad_spec loops. Blocking for Story 1.7, which is its first real consumer.
  closed: RESOLVED 2026-09-02 by Story 1.6a. `src/adapters/fs/walk.ts` is the enumerator, built on a new `ConfinedReader.childrenOf` rather than on the unconfined `list.ts` — AD-10's scoped exception is names-only, no-recursion, no-read, and a recursing reader is outside it. Every property this entry names is now pinned by a named test in `test/adapters/walk.test.ts`: symlinks resolve before confinement is checked (an escaping link is reported `unchecked` at the `confinement` stage, never descended, never read), identity is the resolved real path so one file with two names appears once, a directory cycle terminates with each real directory appearing once, a dangling link is `absent` and the pass continues, and a directory denied `EACCES` is one `unreadable` entry among otherwise complete results rather than the end of the pass — which is `test/support/gate.ts`'s defect stated as an assertion. Bounded in depth and in total entries with the budget a required argument, both bounds asserted from **both** sides.
  mutations: Re-run 2026-09-02 against the new tests, eight of them, all caught. Shrinking the depth cap (`>= maxDepth - 1`) fails 2; widening it (`+ 1`) fails 2; shrinking the entry budget fails 2; widening it (`>` for `>=`) fails 2; making the walk rethrow an unreadable listing instead of reporting it fails 5; removing the per-child confinement re-check fails 5; trusting a `Dirent` for a symlink — the gate walker's original defect — fails 8. Disabling cycle detection fails 3 rather than hanging, because the depth and entry bounds stop it, which is the point of having both.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6a-walk-the-artifact-tree-safely.md`
  summary: (low) `src/adapters/fs/walk.ts` ships in the published bundle with an asserted-**empty** importer set and no consumer, so today it is dead shipped code held in place by a test that says so.
  evidence: Recorded 2026-09-02 by decision. The empty set is deliberate and is the enforcement AD-10 relies on — a new adapter that reads *and* recurses needs its importer set stated exactly, so its first consumer is a deliberate edit in `test/architecture.test.ts` rather than a quiet widening. Story 1.7 is that consumer, and the walk was built one story ahead of it on purpose (the spec's own reason: the walk's failure modes are a different kind of risk from identity classification, and pairing them produced three consecutive bad_spec loops). The cost of being explicit about it: if 1.7 slips or changes shape, this note is the trace, rather than a reader finding an unreferenced module and having to reconstruct why it is there. `esbuild` bundles from `src/cli/index.ts`, so nothing unreachable actually reaches `dist/` — the "ships" half is about the repository, not the tarball.
  triggers: Story 1.7. If it lands without importing the walk, that is a finding about 1.7, not about this entry.
  closed: RESOLVED 2026-09-02 by Story 1.7. `src/cli/inventory.ts` — the identification pass — is the walk's first and only consumer, and `test/architecture.test.ts` now asserts the importer set as `['src/cli/inventory.ts']` rather than as empty, which is the deliberate edit the empty set existed to require. The `triggers` line above was the fence and it held: 1.7 did not land without importing the walk. What the same shape now says one level up is recorded as its own entry under Story 1.7's section — the pass itself has an asserted-empty importer set until Story 1.12 renders the inventory.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-fail-with-the-command-that-would-have-worked.md`
  summary: (low) `Listing`'s failure carries an untyped `reason: string` holding a raw OS message, against the project's closed-vocabulary convention for states.
  evidence: Confirmed in the round, 2026-09-02. It was internal when it was written; the honest-skip reporting applied in this same round makes it **user-facing** — an `EACCES` message from `node:fs` now reaches a terminal inside the scan's incompleteness sentence. AD-8's four-state vocabulary (present, absent, unreadable, unchecked) is the shape the rest of the project uses for exactly this, and `ConfinedReader`'s `Entry` already carries the same untyped `reason` beside it. Deferred rather than patched because doing it once, for both, is a different unit of work from doing it for the newer of the two.
  triggers: Story 1.9, which owns reporting unreadable artifacts in the UI and will have to answer this for `Entry` anyway. Whichever lands first should do both.
  findings_already_measured: Story 1.6a landed first, on 2026-09-02, and answered it **for the new surface only** — so this entry stays open for `Listing` and for `Entry`, which are untouched. What the walk does, and what 1.9 should copy rather than redesign: the *state* is AD-8's closed four (`present`, `absent`, `unreadable`, `unchecked`) and a separate closed `ChildStage` (`confinement`, `resolve`, `read-directory`) says at which stage it failed, which is the half Story 1.9's "naming what failed and at which stage" actually needs. `reason` survives beside them as the raw OS detail, untyped on purpose: a `reason` is what a human reads once the machine-checkable part has already been said in the two typed fields, and typing it would mean either enumerating every errno or paraphrasing the OS. No fifth state was introduced — out-of-tree is a `stage`, not a state.
  correction: 2026-09-02, Story 1.9. The `evidence` above is left standing and is **wrong in its central claim**, which this corrects rather than edits away. It says the honest-skip reporting made this user-facing — "an `EACCES` message from `node:fs` now reaches a terminal inside the scan's incompleteness sentence". Measured today: `src/cli/suggest.ts:361` pushes `toPlatform(directory)` onto `skipped` and **discards `listing.reason` entirely**, and the sentence it builds at `:394-399` names paths and counts — `N could not be read (<path>, <path>)`. No OS message from `list.ts` has ever reached a terminal. So `Listing` was internal when it was written and is internal still, and the urgency that made this a Story 1.9 trigger was not real. The vocabulary question was.
  closed: CLOSED BY DECISION 2026-09-02 by Story 1.9, which answered the **question** rather than retyping both values. The prescribed shape now exists as code, in one place: `src/domain/signal.ts` holds AD-8's four states (`present`, `absent`, `unreadable`, `unchecked`) as one shared vocabulary plus a closed `ReadStage` (`confinement`, `resolve`, `read-directory`, `examine`, `read`, `decode`), and `Readability` pairs a typed state with a typed stage and keeps the platform's own words beside them in an untyped `reason` — untyped for exactly the reason `findings_already_measured` above gives. `ConfinedReader.readText` is ported to it: its failure carries a typed `state` **and** a typed `stage` beside `reason`, each narrowed with `Extract` to what that path can actually produce, so an over-limit refusal, a failed open and a failed decode are distinguishable without pattern-matching English — which is the half of "naming what failed and at which stage" that had no answer. Returning the state as well as the stage was review round 1's correction and it removed a real divergence: the pass had hardcoded `unreadable` for every read failure, so a file that vanished between the walk and the read was `unreadable` while the identical condition noticed one step earlier was `absent`. `ChildStage` is now `Extract`ed from `ReadStage` rather than spelled a second time, and `src/cli/inventory.ts` records the resulting signal on every entry instead of flattening a state and a stage into prose. The two values this entry names are **left as they are, by decision**: `Listing` has one consumer, which discards its reason (see the correction above), and `Entry` has one, `resolveLocation`, which maps every kind into `Refusal` — this project's own closed cause-plus-raw-text vocabulary — so a typed state on `Entry` would be re-mapped into another typed state at its only call site. Porting either would add vocabulary nobody consumes, which is the unconsumed-surface shape this project has been caught with three times. The residue is recorded as its own entry under Story 1.9's section, with the trigger that makes it due.
  mutations: Re-run 2026-09-02 against the resolution, both directions of the thing this entry is about. Reintroducing the flattening — `unusable` returning `` `${state} at the ${stage} stage: ${reason}` `` — fails `test/cli/inventory.test.ts`'s "the state and the stage stay typed, and no reason is a sentence about them", and nothing else, which is the right blast radius for a formatting regression. Reporting an unread file as readable — `UNREAD` set to `{ state: 'present' }` — fails three: "an undecodable file under an artifact root is unchecked, never present" and "over this repository, only what a level read is reported readable" in the pass's tests, and "a signal that read nothing carries no stage, and does not claim the content is fine" in the vocabulary's. Worth recording, because it is the type rather than a test doing the work: the *narrower* mutation, keeping the reason beside `state: 'present'`, does not typecheck at all — the `present` variant of `Readability` carries no `reason` field to put it in.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-identify-what-each-artifact-is.md`
  summary: COLLISION, unresolved by decision — `EXPERIENCE.md:170` calls an unidentified artifact "present-but-uninterpreted", which is FR-12's term for a *different* thing. The epics split the two; the UX document merges them.
  evidence: Recorded 2026-09-02 rather than resolved, because resolving it means editing a normative planning document and picking which of two vocabularies wins, which is not an implementation decision. What is true today: FR-8/FR-69 own **unidentified** — no precedence level resolved what an artifact is — and FR-12 owns **present-but-uninterpreted** — a shape the tool does not recognize is listed rather than hidden. `EXPERIENCE.md:170` writes "listed as present-but-uninterpreted, naming the levels tried (FR-69)", which is both terms in one sentence. Story 1.7 keeps them apart: `src/domain/identity.ts` uses `outcome: 'unidentified'` and says in its header that the presentation term is Story 1.9's. Nothing in code carries the second term yet, so the collision costs nothing until 1.9 renders it.
  triggers: Story 1.9, which owns FR-12's presentation and is the first place both terms have to be spelled on one surface. It should either reconcile the two documents or state which term the UI uses and why.
  closed: RESOLVED 2026-09-02 by Story 1.9 — the story the `triggers` line named, and it took the first of the two options that line offered rather than editing a normative document. **Decision: the two terms stay distinct, and neither resolves the other by precedence.** FR-12 is *recognized, but no interpretation applies*; FR-69 is *identification itself failed*. Both definitions are recorded as data in `src/domain/interpretation.ts`, quoted verbatim from `prd.md:177` and `prd.md:164`, and `test/domain/interpretation.test.ts` reads both out of the PRD rather than restating them, so a reworded requirement fails the suite instead of leaving two vocabularies standing. The rule that decides which applies consumes the **recorded verdict** and derives nothing (AD-4): an `unidentified` outcome is FR-69, any other outcome whose every reading carries `shape: 'unknown'` is FR-12, and everything else is neither. Why distinct rather than merged: they answer different questions and an artifact can be in either without being in the other — an unremarkable `.txt` no level resolves is FR-69 and says nothing about shapes, while a directory of markdown with no `index.md` is identified by name or location and still has no shape any view can interpret, which is FR-12 exactly. Collapsing them would report "not identified" over an artifact the authority identified at `certain`, which is the false claim AD-7's typed values exist to prevent; `review-adversarial-seams.md:352-359`'s single closed enumeration was considered for the same reason and **not adopted**. `EXPERIENCE.md` is deliberately **not edited**: the copy for FR-69 is already in its string index (`Not identified. Tried: config path, frontmatter, structure, filename.`), the merged sentence at `:170` is left standing as prose the code does not follow, and the fact that FR-12's state has no row of its own in that index is recorded as a new entry under Story 1.9's section, against Story 1.12, rather than answered by inventing page copy in a domain module.
  mutations: Re-run 2026-09-02, twice — the second time in review round 1, which found that the *distinction* was pinned and its **applicability** was not. Applying an interpretation unconditionally reported a dangling `gone.md` as `present-but-uninterpreted` beside its own `state: absent`, and changing that value to anything at all left the suite green, so neither the value nor any replacement was observed. Now guarded in `src/cli/inventory.ts` (`undefined` for an entry the walk did not report present) and asserted in both directions in three rows of `test/cli/inventory.test.ts`; removing the guard fails all three. Collapsing FR-12 into FR-69 — `interpret` returning `unidentified` where no reading carries an interpretable shape — fails five tests across two files: "a recognized family with no interpretable shape is FR-12, not FR-69", "an ambiguity whose axes crossed carries no shape, so it is FR-12" and "every state has exactly one recorded definition, and no state is unreachable" in `test/domain/interpretation.test.ts`, and "FR-12 and FR-69 arrive as distinct states on the entry" and "over this repository, only what a level read is reported readable" in `test/cli/inventory.test.ts`. The last one is the interesting failure: it enumerates *which* rows over this repository's own tree are FR-69 — the three BMAD layout directories, and nothing else — so a collapse shows up as a real project's inventory changing, not only as a unit assertion.
