---
epic: 1
date: 2026-09-03
verdict: rejected
criteria: declared
headless: false
---

# Epic 1 Retrospective — Point it at a project and see what's there

## Epic summary

**Epic:** 1 — "Point it at a project and see what's there" (`_bmad-output/planning-artifacts/epics.md:248-256`).

**Diff range:** `4b825dc642cb6eb9a060e54bf8d69288fbee4904..50387ca` — the empty-tree sentinel to `HEAD`. The first story commit `b7eb278` is the **repository root** and has no parent, so the reference's `<first>^..<last>` form is unavailable; the empty tree is its stand-in. 25 commits, 0 merges (`merge_count: 0`, `merges_measured: 0` — consistent), `binary_revisions: null`.

**Stories:** 13, all `done`. `pending_stories: []` — the epic is genuinely complete, so no unfinished-story override applies to the verdict.

Order: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, **1.6a** (carved out of 1.5 mid-epic at a multi-goal checkpoint), 1.7, 1.8, 1.9, 1.10, 1.11, 1.12.

### Evidence inventory

| Evidence | Available | Notes |
|---|---|---|
| Epic spec | yes | `epics.md:248-256`; per-story acceptance in `epics.md:293-399` |
| Story specs | 13 of 13 | `_bmad-output/implementation-artifacts/spec-1-*.md`, each with frozen intent, Code Map, Spec Change Log |
| Diff range and commits | yes | see above |
| Per-story commit attribution | **NO** | every commit returns `stories: []` — see Finding P1 |
| Sprint status | yes | `sprint-status.yaml`; retro key `epic-1-retrospective`, was `optional` |
| Previous retrospective | none | Epic 1 is the first; no follow-through section applies |
| Session logs | partial | four `.memlog.md` files, all from **planning** phases (PRD, architecture, UX, spec). No implementation-session logs — see Finding P2 |
| Deferred-work log | yes | `deferred-work.md`, 500+ lines, the epic's own running record of decisions, corrections and deferrals |

### Change volume by area (whole range)

| Area | Added | Deleted | Files |
|---|---|---|---|
| `test` | 20,889 | 696 | 40 |
| `src` | 11,684 | 387 | 27 |
| `_bmad-output` | 11,160 | 326 | 42 |
| `scripts` | 572 | 47 | 3 |
| `.claude` | 24,586 | 0 | 248 |
| `_bmad` | 1,541 | 5 | 22 |

**Scope caveat, load-bearing for every number above.** The root commit `b7eb278` bundles three unrelated things: 15 product files / 3,310 lines (story 1.1), 269 files / 26,084 lines of BMAD toolchain install (`.claude/`, `_bmad/`), and 29 files / 7,948 lines of prior-phase planning artifacts. Roughly 92% of that commit is not Epic 1's work. Read `.claude` and most of `_bmad` as scaffold; the epic's product contribution is `src` + `test` + `scripts`.

## Findings

Every finding carries its source. Each has two dispositions: **this instance** (fix now / defer / accept) and **upstream** (what would prevent the next one). Sub-agent reports were re-checked against the primary source before routing; one was dropped (see Evidence quality, below).

### A — Architecture delta

**A1. The ports ring was never built; the paradigm has no mechanical representation.** `src/ports/` is in the declared layer table (`ARCHITECTURE-SPINE.md:29`) and the dependency diagram (`:44`), and three declared edges depend on it (`:55` `FS --> PORTS`, `:56` `GIT --> PORTS`, `:57` `PORTS --> DOMAIN`). `git log --diff-filter=A --all -- 'src/ports/*'` is empty across all 25 commits. `src/adapters/fs/read.ts:101` imports `src/domain/signal.ts` directly, skipping the mandated hop. The spine's own frontmatter says `paradigm: hexagonal (ports and adapters)` (`:6`). 4 of the 10 declared edges exist; 6 do not.
*This instance:* **reconcile spec** — either build the ring or amend the table to describe the three-layer shape actually built. *Upstream:* a declared layer with no test asserting it exists is a layer that will not get built.

**A2. One unambiguous layering violation, unenforced.** `src/adapters/http/server.ts:44` imports `src/adapters/fs/paths.ts`. Table row `:31` grants `src/adapters/http/` exactly "ports, domain, render"; the diagram has no `HTTP --> FS` edge. No test constrains what the HTTP adapter imports — `grep "startsWith('src/" test/` returns 3 hits, none of them an http rule.
*This instance:* **fix now** (add the rule; decide whether to keep the import). *Upstream:* the gate enforces AD-1 and purity but has no general layer-direction check.

**A3. `src/render/` "domain types only" is violated three ways.** `src/render/chrome.ts:38` imports `node:path`; `src/render/inventory.ts:56,57,59` import domain **values** (`SIGNAL_LABELS`, `outOfTreeReport`, `fillIndexString`), not types. Only the render→cli direction is enforced (`test/architecture.test.ts:1222`), and its own comment records it was added after a measured hole.
*This instance:* **accept and record** the value imports (reading normative tables is the intended design), **fix** the wording or the `node:path` import. *Upstream:* "types only" was never mechanically expressible as written.

**A4. Enforcement coverage is inverse to actual coupling.** `src/adapters/fs/paths.ts` (fan-in 7 — the most cross-cutting module in the tree) and `read.ts` (fan-in 4) have no importer allowlist; `list.ts`, `walk.ts` and `segments.ts` (fan-in 1 each) all do.
*This instance:* **fix now** — cheap. *Upstream:* importer sets were added reactively, one per story, so they cover what a story touched rather than what is most coupled.

**A5. Two gate permissions are vacuous and asserted as load-bearing.** `test/support/gate.ts:28` grants `src/adapters/git/` a `child_process` allowance over a directory that has never existed, and `test/architecture.test.ts:587` pins that grant by `deepEqual`. `web/` is in `SCANNED_ROOTS` (`gate.ts:24`) and does not exist, so the `:34` row is unenforceable and passes on an empty walk.
*This instance:* **fix now.** *Upstream:* same class as the epic's dominant defect — an assertion that cannot fail.

**A6. No cycle or reachability check exists.** The graph is a DAG today (26 nodes, 55 edges, Tarjan SCC all size 1) but that is a property of the current tree, not an invariant.
*This instance:* **defer.** *Upstream:* n/a.

**A7. The gate's own specifier scanner over-matches prose ending in "from".** `test/support/gate.ts:71` yields a phantom specifier from `src/adapters/fs/walk.ts:550` (`'…the directory the walk started from'`). Inert today, because the garbage keys to neither gated module — but the same construction inside any `src/domain/` file would be reported as a **false purity violation**, and it is counted by the liveness check at `architecture.test.ts:852`.
*This instance:* **fix now.** *Upstream:* the scanner is unit-tested (`test/support/scanner.test.ts`) but not for this case.

### B — Duplication map

**B1. `errorCode()` is byte-identical in two modules, unexplained, and deferred 13 stories ago.** `src/adapters/http/server.ts:449-455` and `src/cli/index.ts:904-910` — `diff` of the two bodies is empty. Both module-private; `grep errorCode test/` returns zero hits. Recorded at `deferred-work.md:38` as "Cleanup, not correctness; deferred so it does not compete with the correctness work in this story" — still open. A third spelling of the same narrowing is `test/support/gate.ts:322` `isEnoent`.
*This instance:* **fix now.** *Upstream:* "cleanup, not correctness" deferrals do not age out on their own.

**B2. `fill` / `fillIndexString` — deliberate duplication that then diverged, twice, with neither fix back-ported.** `src/cli/suggest.ts:152` vs `src/render/html.ts:190`. The duplication is explained in both source (`html.ts:171-176`) and `deferred-work.md:533`, and the reason is sound (render may not import from `src/cli/`). But `fillIndexString` received two corrections `fill` did not: the `Object.hasOwn` fix and the widened `[A-Za-z][A-Za-z0-9_-]*` pattern. **`suggest.ts` still throws for a target directory whose name contains an angle bracket** (`deferred-work.md:555`, open, two-line fix).
*This instance:* **fix now** (the `suggest.ts` throw is a live defect). *Upstream:* a documented deliberate duplication needs a test asserting the two agree, or it drifts — and this one drifted in the direction of the copy nobody was looking at.

**B3. Path decomposition implemented three times with two incompatible sentinels** — `src/cli/inventory.ts:398` returns `parent: '.'`, `src/domain/identity.ts:762` returns `''`, and `identity.ts:756` is the `name` half. All three share the same `lastIndexOf('/')` line. The **join** half was deliberately exported to prevent exactly this (`src/adapters/fs/walk.ts:319-328`: "a second copy of this one-liner is a second spelling of the walk's own convention, free to drift").
*This instance:* **defer** (the sentinels are load-bearing where they are). *Upstream:* the reasoning that produced `childRelative` was not applied to its inverse.

**B4–B7. Test-helper duplication against helpers that already exist.** The regex-escape literal appears 6× byte-identical across 5 files, named once (`test/server.test.ts:374`). `makeScratchDir` exists and its own header says the private copies were why it was written (`test/support/project.ts:44`) — 5 private copies survive, one of them in a file that already imports the module. `whileDenied` restores the **observed** mode and explains why (`test/support/tree.ts:201`); 7 hand-rolled sites restore a guessed `0o755` — a behavioural divergence, not a style one. `deniableDirectories()`'s predicate is inlined verbatim at 7 sites.
*This instance:* **fix now** for `whileDenied` (behavioural), **defer** the rest. *Upstream:* `test/` is deliberately ungated, so nothing detects helper duplication there.

**B8–B9. Constants duplicated without a cross-check.** `MAX_PORT` is defined twice with different literal spellings (`server.ts:55` `65535`, `cli/index.ts:49` `65_535`), both private, and the value is restated in four test expectations. `'_bmad-output'` exists independently as a `MARKERS` element (`cli/location.ts:34`) and as `OUTPUT_DIRECTORY` (`cli/inventory.ts:148`); no test asserts `MARKERS.includes(OUTPUT_DIRECTORY)`.
*This instance:* **fix now** (cheap). *Upstream:* the repo has exactly **one** test asserting two implementations agree (`package.json` ↔ `src/version.json`, `test/cli-entry.test.ts:400`) and one exemplary pattern (`BREAKPOINT_PX` read out of `DESIGN.md`, `test/render/stylesheet.test.ts:316`). Neither was generalized.

### C — Size and structure growth

**C1. `src/domain/identity.ts` — 1369 lines, with a clean 324-line extraction available.** Lines `:403-726` are pure rule data (12 declarations); the only two call expressions inside the region reference tables declared in the same region, so the dependency runs one way. 24% of the file's lines, 0% of its behaviour. Growth: 69% arrived in story 1.7, a further 332 net in 1.10.
*This instance:* **defer** (cohesive today). *Upstream:* n/a.

**C2. `src/cli/inventory.ts` — 1104 lines; its header claims 3 concerns, admits a 4th, and the code has 6.** The undeclared ones are alias restoration (`:467`) and sprint-tracking resolution (`:963-1104`). The sprint block joins the rest of the file through **exactly one edge** (`:760`) and arrived whole in one commit (`e7b6c7a`, +219).
*This instance:* **defer**, with the split boundary recorded. *Upstream:* the module's header was not updated when story 1.11 added a concern to it.

**C3. `src/cli/index.ts` — 947 lines, four concerns, header names three.** It sat at 602 lines through six stories then grew **57% in the final story** (`50387ca`, +347): region B, the inventory→view projection (`:332-596`), which the header does not claim and which is a render-layer adapter living in the composition root. Nine of its eleven identifiers are used exactly once, inside the file; its single production edge to the rest is `:792`. The reason it lives there is documented in a *different file* (`src/render/inventory.ts:11-16`).
*This instance:* **defer**, boundary recorded. *Upstream:* the layering constraint that forced this placement was discovered at spec time and resolved by putting the code where the constraint allowed rather than where it belongs.

**C4–C5. Two test files are two suites stapled together.** `test/architecture.test.ts:754-1275` (521 lines, ~20 tests) sits outside every banner and is a per-module importer registry that grew one entry per story — 13 of 26 commits touched this file, the widest spread in the epic. `test/server.test.ts:745-1015` (~270 lines, 11 tests) tests `src/cli/index.ts`, not the server, and overlaps three dedicated CLI test files that already exist.
*This instance:* **defer.** *Upstream:* per-story review rounds see a file's diff, never its accumulated shape.

**C6. The headline test:src ratio is misleading.** Line-based it is 1.79:1 (20,889 : 11,684). But `src/` is 20–56% comment-free code (median ~38%) against 61–72% in `test/`, so on code lines the ratio is closer to **3.4:1**. Any future metric on this repo needs the density caveat.
*This instance:* **accept and record.** *Upstream:* n/a.

**C7. `src/cli/index.ts` is the only `src/` module with no name-matched test file** — its coverage is distributed across ≥10 files with no owner, and its four concerns map to four different test files. Every other `src/` module is imported by at least one test; there are zero orphans.
*This instance:* **defer.** *Upstream:* n/a.

**C8. Zero `describe(` blocks exist anywhere; all 40 test files are flat lists**, grouped only by `// ---` banners where at all. Two of `test/cli/inventory.test.ts`'s five banner sections are named after **stories** rather than subjects, and its sections map 1:1 to commits.
*This instance:* **accept.** *Upstream:* chronological test organization is what an epic of sequential sessions produces by default.

### D — Spec-to-implementation reconciliation

**D1. FR-10 is contradicted in five places, and AD-9 contradicts itself within one AD.** `prd.md:168` carries FR-10 with no deferral note. `epics.md:194` and `:253` list it as Epic 1 coverage; `:325` says Story 1.5 satisfies it. `deferred-work.md:147` records `DEFERRED INDEFINITELY 2026-09-02`. And `ARCHITECTURE-SPINE.md:118` — AD-9 rule 1 — still reads "Artifact roots are read once from the target project's own configuration", two rules above `:120`'s note that FR-10 is deferred. No file under `src/` reads any config (`grep -rn "config.yaml" src/` → four comment lines).
*This instance:* **reconcile spec — human only.** Already recorded as a finding at `deferred-work.md:477` with a `revisit`. Implementers correctly declined to edit binding documents. *Upstream:* a deferral decision needs a single owner for propagating it into every document that asserts the opposite.

**D2. The story list diverges from the epic file in both directions.** `epics.md:293-399` names 12 stories; 13 were built (`1-6a-walk-the-artifact-tree-safely`, split from 1.5 mid-epic by user decision, `deferred-work.md:670`) and `epics.md` was never amended to name it. A 14th (the FR-10 config story) was split out and never built.
*This instance:* **reconcile spec.** *Upstream:* mid-epic story splits update `sprint-status.yaml` but nothing updates `epics.md`.

**D3. Epic 1 declares no epic-level acceptance criteria; it declares a *posture* about where they live.** `grep -n "cceptance" epics.md` returns exactly one hit, `:275`: "Acceptance criteria are stated lean: each story names the requirements it satisfies and one condition for done. The requirements themselves are the contract… Visual appearance and perceived performance are judged directly by the product owner rather than specified as criteria." So the declared criteria set is composite: **the 24 FRs at `:253` + the NFR/UX-DR assignment at `:269` + the 12 per-story done-conditions**. The verdict below is against **declared** criteria on that reading.
*This instance:* **accept and record.** *Upstream:* n/a — this is a deliberate, documented choice.

**D4. Five declared criteria are documented as unmet.** FR-10 (D1). FR-71's stated check — `epics.md:379` "a folder containing two same-day runs of one family reports both" — `deferred-work.md:375` records it "undeliverable from a folder's contents and is not delivered". NFR-14 keyboard operation, claimed at `epics.md:269` and in Story 1.12's own done-condition (`:399` "keyboard navigable") — `deferred-work.md:523`: "**Nothing on this surface is keyboard-operable, and nothing tests keyboard or ARIA anywhere in the project.**" NFR-6/AD-13 — `deferred-work.md:339`: "AD-13 has no code to bind here". NFR-17 — `deferred-work.md:437`: "**both** halves of NFR-17's named surface are deferred, and only `story_location` is covered".
*This instance:* **defer, tracked** — every one is already recorded with a trigger. *Upstream:* four of the five were caught and recorded by the story that could not deliver them, which is the process working; FR-10 is the one that was claimed as covered anyway.

**D5. The family vocabulary is now twelve and no record says twelve.** `src/domain/identity.ts:133-140` — `RunFolderFamily` (7) plus `review`, `epics`, `story`, `sprint-tracking`, `note`. Entries record the widening "from FR-11's seven to eleven" (`deferred-work.md:266`) and that `review` was added (`:397`), but not the current count. The spine's conventions row (`ARCHITECTURE-SPINE.md:193`) names eight. `note` appears nowhere in `prd.md`, the spine, `epics.md` or `EXPERIENCE.md`. Measured live: 12 family tiles render.
*This instance:* **reconcile spec.** *Upstream:* a vocabulary widened once per story, with each widening recorded relative to the last, loses its absolute count.

**D6. Eleven numeric caps and budgets are invented and not recorded as such.** `INVENTORY_BUDGET` (12/4000), `MAX_READ_BYTES` (8 MiB), `MAX_SEGMENT_BYTES` (255), `MAX_PATH_BYTES` (4096), `MAX_COMPONENTS` (64), `MAX_RECORDED_SUPPRESSIONS` (200), `MAX_RECORDED_SKIPS` (200), `MAX_CHILDREN` (128), `MAX_PROBES` (512), `MAX_SUGGESTIONS` (10), `SHUTDOWN_TIMEOUT_MS` (2000). The only numbers the planning corpus supplies are FR-70's "two levels below" and `DESIGN.md`'s 900px. These are load-bearing on what the page reports (`deferred-work.md:543`).
*This instance:* **defer, but record them as invented** — the epic recorded far smaller inventions and missed these. *Upstream:* a cap invented inside a story is invisible to that story's review, which sees only whether it is tested.

**D7. UX-DR3's "Load IBM Plex Sans, Mono and Serif" is not implemented and the divergence is recorded nowhere.** `src/render/tokens.ts:97` "No font file is shipped and none is fetched"; `test/render/page.test.ts:179` asserts nothing is fetched. The code is defensible under UX-DR24 precedence, but this is the one criteria divergence in the epic with **no** `deferred-work.md` entry.
*This instance:* **reconcile spec.** *Upstream:* the deferred-work discipline caught everything else; this slipped because a *test* asserts the opposite of the requirement and nobody noticed the conflict.

**D8. Four AD amendments; three are undated and one contradicts a document that was not updated.** AD-9's narrowing is dated in the spine (`:120`, 2026-09-02) and echoed in `epics.md:140`. AD-10's scoped exception (`:127`) is **undated everywhere**, and `epics.md:141` still states AD-10 in its unamended form. AD-19's second rule and AD-1's amendment are recorded only in the architecture `.memlog.md:63-64`, undated. And `ARCHITECTURE-SPINE.md:8-10` still reads `status: final` / `updated: '2026-08-28'` while the body carries a rule dated 2026-09-02.
*This instance:* **reconcile spec.** *Upstream:* amendments were made where they were noticed, not through one channel that stamps them.

### E — Cross-story seams (the defects no single story could see)

Each was demonstrated by mutation against the **green 834-test baseline**. Every mutation was reverted; the tree was verified clean afterwards (see Evidence quality).

**E1. LIVE DEFECT — an unreadable project root is reported as a finished scan of an empty project. No mutation needed.** `Inventory.startEntry` (`src/cli/inventory.ts:281`) is documented at `:274` as "kept rather than dropped because its state is the one that says whether the pass could read anything at all". `projectInventory` (`src/cli/index.ts:383-455`) never reads it, and narrows `complete` to three counters on the argument. **Reproduced end to end by the retrospective author**, on a root with mode `0111`:

```
Inventory.startEntry.state : unreadable (stage read-directory)
Inventory.complete         : false        entries: 0
view.complete              : true         artifactCount: 0
page: "The scan finished. Artifacts examined: 0."
      "A BMAD project, with no artifacts yet."
```

The narrowing argument — that per-entry failures "are already on the affected entry's own row" — fails precisely here, because the start entry is deliberately kept **out** of `entries` and so has no row. This is the same "the one loss the narrowing does not cover" reasoning that `test/render/inventory.test.ts:514` applies to aliases, unapplied to the root.
*This instance:* **fix now — blocking.** *Upstream:* the projection was reviewed as a new module; nothing reviewed what the pass records that the projection discards.

**E2. Run-folder facts: only the array *length* crosses the projection.** `test/render/inventory.test.ts:421` asserts `row.runFacts.length === entry.runFacts.length` — never `measured`, `reuse` or `dateSignal`. Mutations at 834/834 green: `rowRunFacts` returning `{measured:false}`; and flipping `reuse` deliberate↔accidental. Live on this repository (4 measured readings; all four sentences render today). Consequence of the flip: the page tells the reader a same-day rerun is a deliberate resume when it is an accident of the naming.
*This instance:* **fix now.** *Upstream:* asserting a collection's length is the classic shape of an assertion that cannot fail.

**E3. An identified verdict's `shape`, `confidence` and `resolvedAt` never cross the projection.** Three separate one-line mutations, each 834/834 green: `shape:'unknown'`, `confidence:'certain'`, `resolvedAt:'filename'`. The real-tree loop (`test/render/inventory.test.ts:403-422`) asserts four fields and its comment claims "every row keeps the four things the pass recorded" — these three are not among them. `confidence:'certain'` removes FR-69's below-certain caveat from the page entirely.
*This instance:* **fix now.** *Upstream:* the loop enumerates what the story that wrote it cared about, not what the row carries.

**E4. The `absent` and `out-of-tree` story-location branches are each deletable with the suite green.** `src/cli/index.ts:576-579`. With `absent` gone, every project without a `sprint-status.yaml` — the shape FR-75 calls **normal** — renders `Sprint view unavailable. absent` instead of `No sprint tracking in this project.` The function's own docstring records this defect class having already been found once for `in-tree`; the branches added to fix it are themselves unpinned.
*This instance:* **fix now.** *Upstream:* a fix added in review needs its own test, not the reviewer's word.

**E5. Both overflow counters are mirror-asserted on a tree where both are zero.** Dropping `suppressedNotRecorded === 0` from `complete`, and dropping `+ skippedNotRecorded` from `namesLeftOut`, each leave 834/834 green — because on this repository both terms are `0`. The producing end *is* covered (`test/cli/inventory.test.ts:615`, `:948`, `:1176`); neither of those tests calls `projectInventory`.
*This instance:* **fix now.** *Upstream:* a mirror assertion over a fixture where the term is zero is a tautology.

**E6. The output folder's case-insensitive exclusion has no test behind it.** `src/cli/index.ts:407` lowercases **both** sides and its comment argues that lowering one "happened to work and would stop working the moment it was not". Replacing it with a case-sensitive compare leaves 834/834 green. Reachable on a case-sensitive filesystem, because the pass matches case-insensitively while the projection would not.
*This instance:* **fix now.** *Upstream:* a comment that explains why a subtlety matters is the strongest available signal that it needs a test.

### F — Cross-story edge cases

From the edge-case lens over the composed runtime path, each with a proposed guard. Not individually re-reproduced by the author; routed as **defer** pending confirmation, except where noted.

**F1.** The output-folder skip drops the entry even when it is not `present`, so an unreadable artifact tree loses its only carrier of that fact (`src/cli/index.ts:407`) — same family as E1, **fix now** alongside it.
**F2.** `src/adapters/fs/walk.ts:451-517` — when `slots < 1` and the directory's one kept child is suppressed as an alias, no truncation is recorded, so a short listing is reported available and a run-folder verdict can be `certain` over an unseen `index.md`.
**F3.** `src/cli/inventory.ts:1010` — if the walk truncated before reaching `sprint-status.yaml`, the page asserts `No sprint tracking in this project.` over an unfinished scan.
**F4.** `src/cli/inventory.ts:963` — a `sprint-status.yaml` the walk suppressed as a second spelling yields "no sprint tracking" for a project that has one.
**F5.** `complete` does not consider `aliases.every(restored)`.
**F6.** `src/cli/inventory.ts:373` — root-level skips spend the record budget, so a complete scan can report "the scan did not finish".
**F7.** `src/cli/inventory.ts:1065` — a `story_location` after a mid-file `---` reports `undeclared`, claiming the file declares nothing when it declares a location.
**F8.** `src/render/inventory.ts:700` — an alias whose `reportedAt` is the start directory renders "a second spelling of `.`", naming nothing.

### G — Process findings

**P1. Commit subjects carry no story attribution, so churn cannot be mechanically attributed.** `git_evidence.py` returned `stories: []` for all 25 commits against all 13 story ids. Subjects are prose (`feat: identify what each artifact is`).
*This instance:* **accept** for this epic. *Upstream:* **fix** — include the story key in the commit subject or trailer, so a retro can attribute change volume without hand-mapping.

**P2. No implementation-session logs exist.** The four `.memlog.md` files are all planning-phase (PRD, architecture, UX, spec). Process-lesson analysis therefore rests on the specs' own Spec Change Logs and `deferred-work.md`, both of which proved unusually rich — but the record of what was *tried and abandoned* mid-session does not exist.
*This instance:* **accept and record.** *Upstream:* nothing in the build workflow writes a session log.

**P3. The root commit conflates three unrelated things.** `b7eb278` is 316 files / 37,917 lines: 15 files / 3,310 lines of story 1.1 product code, 269 files / 26,084 lines of BMAD toolchain install, 29 files / 7,948 lines of prior-phase planning artifacts. ~92% is not Epic 1's work, and it has no parent so the reference's `<first>^..<last>` range form is unavailable.
*This instance:* **accept.** *Upstream:* scaffolding a repo and landing the first story in one commit costs every later measurement its precision.

**P4. The dominant defect class across the epic was an assertion or comment claiming a guarantee the code did not provide.** Instances found and fixed during the epic, each recorded: `ChildStage` "pinned by the typecheck" (was not); `LEVEL_LABELS` "pinned against the index" by a test comparing to a literal; `INVENTORY_BUDGET`'s "two orders of magnitude" (actually 2.4×); "nothing outside the root is touched" while issuing a `readlink` on `/etc`; three comments asserting a page load builds a new snapshot while the page was frozen at bind time. This retro adds A5 and A7 to the list.
*This instance:* n/a. *Upstream:* the pattern is now well enough evidenced to be worth a standing check — when a comment asserts a mechanism, the reviewer's first question is which test fails if the mechanism is removed.

**P5. A fix reintroducing its own defect through its bounds was the second recurring class.** Two instances: the alias restore worked for the first 200 suppressions and reopened the hole for the 201st; the slug fix reached levels 1 and 4 and left levels 2 and 3 resolving *at higher confidence* than the reading it replaced.
*Upstream:* when a fix introduces a bound, the bound needs its own row in the matrix.

**P6. The specs' own matrices were where defects landed.** Three stories (1.8, 1.9, 1.10) had their principal defect in a combination the spec's I/O matrix did not enumerate; the rows that were written were built correctly. One story (1.7) had a `bad_spec` — a Code Map that omitted `implementation-artifacts`, making a third of a real tree unidentifiable.
*Upstream:* matrix completeness is the highest-leverage spec quality attribute in this workflow, and it is currently unchecked.

## Behavior verification

Exercised against the **built** CLI (`dist/`), not the sources, and against throwaway trees plus this repository itself.

| Flow | Observed |
|---|---|
| `--help`, `--version` | exit 0 each |
| absent path | exit 2 |
| non-project directory | exit 2, and the suggestion `npx bmad-dash <path> --no-open` — npx-spelled, quoted, carrying the flag that was typed |
| real project, `GET /` | 200, URL on stdout, `Target:` on stderr, `cache-control: no-store` |
| `POST /` | 405 with `Allow: GET, HEAD` |
| `Host: evil.test` | 403 |
| unknown route | 404 |
| `story_location: /etc` | `Story location points outside the project: /etc. Not read.` |
| refresh | 4 rows → add a file → 5 rows → remove it → 4 rows, restoring byte-for-byte |
| invalid UTF-8 outside an artifact root | `Unreadable` + stage `decode` |
| invalid UTF-8 under an artifact root | `Not checked` — Story 1.9's accepted limit, visible on screen |
| hostile filename | verified separately with `<img src=x onerror=alert(1)>.md`: zero raw `<img>` elements, rendered as `&lt;img …&gt;` |
| **unreadable project root** | **"The scan finished. Artifacts examined: 0." + "A BMAD project, with no artifacts yet." — see E1** |

One fixture in the scripted pass failed to create (`<b>bad</b>.md` — the shell interpreted the `>`), so that row of the scripted run is **inconclusive rather than clean**; escaping was verified separately with the filename above.

### Evidence quality

Two things a reader should weigh:

- **Two mutation-running lens agents were launched in parallel on one working tree** — the retrospective author's error. One detected the other's in-flight mutation of `src/domain/interpretation.ts`. Both restored correctly; afterwards `git diff` was empty, `git status` showed only this document, and the suite was at its 834/834 baseline — all verified rather than assumed. No finding in section E or F depends on the concurrently-mutated file.
- **One sub-agent finding was dropped**, not routed: an apparent ambiguous-run-folder rendering defect turned out to be a stale `dist/` artifact and disappeared after a rebuild. The agent reported the discard itself.
- Section F findings were **not** individually re-reproduced by the author and are routed as deferred pending confirmation, except F1 which is the same family as the reproduced E1.

## Previous-retro follow-through

Not applicable: Epic 1 is the first epic, and no prior retrospective exists.

## Action items

All twelve tracked in `sprint-status.yaml` (`action_items`), each pointing back to its sourced finding in this document. **Remediation and spec reconciliations are proposed, not applied** — the retrospective does not run the dev loop and does not edit the project contract.

| # | Action | Owner | Kind | Finding |
|---|---|---|---|---|
| 1 | **[BLOCKING]** Carry `Inventory.startEntry` and the pass's `complete` into the view, so an unreadable project root is not reported as a finished scan of an empty project | dev loop | remediation | E1, F1 |
| 2 | Pin the five unpinned projection seams: run-facts values, an identified verdict's `shape`/`confidence`/`resolvedAt`, the `absent`/`out-of-tree` location branches, both overflow counters against non-zero fixtures, and the case-insensitive output-folder exclusion | dev loop | remediation | E2–E6 |
| 3 | Fix `src/cli/suggest.ts`'s private `fill`, which throws for a target directory whose name contains an angle bracket — two lines, mirroring `fillIndexString` | dev loop | remediation | B2 |
| 4 | Close the architecture-gate gaps: a layer rule for `src/adapters/http/`, importer sets for `paths.ts` and `read.ts`, removal of the vacuous `src/adapters/git/` and `web/` permissions, and the specifier scanner's prose over-match | dev loop | remediation | A2, A4, A5, A7 |
| 5 | Consolidate the duplications that carry behavioural divergence: `errorCode` ×2, the seven hand-rolled deny/restore sites that guess `0o755`, and cross-checks for `MAX_PORT` and `'_bmad-output'` | dev loop | remediation | B1, B6, B8, B9 |
| 6 | **[human]** Reconcile FR-10 across all five places at once: `prd.md:168`, `epics.md:194`, `epics.md:253`, `epics.md:325`, and AD-9 rule 1 at `ARCHITECTURE-SPINE.md:118` | Jamie | spec reconciliation | D1 |
| 7 | **[human]** Reconcile the epic file with what was built: add `1-6a` to `epics.md`, and state the family vocabulary's current count of twelve against the spine's eight | Jamie | spec reconciliation | D2, D5 |
| 8 | **[human]** Reconcile UX-DR3's "Load IBM Plex Sans…" clause with the no-fetch implementation, and date the AD-10, AD-19 and AD-1 amendments; update `ARCHITECTURE-SPINE.md`'s frontmatter, which still reads `updated: '2026-08-28'` while the body carries a 2026-09-02 rule | Jamie | spec reconciliation | D7, D8 |
| 9 | **[human]** Decide the ports ring: build `src/ports/` or amend the layer table and diagram to describe the three-layer shape actually built | Jamie | spec reconciliation | A1 |
| 10 | **[process]** Put the story key in each commit subject or trailer, so a retrospective can attribute change volume mechanically | Jamie | process | P1 |
| 11 | **[process]** Standing review check: when a comment claims a mechanism, name the test that fails if the mechanism is removed — the epic's dominant defect class, seven instances | Jamie | process | P4 |
| 12 | **[process]** Treat I/O-matrix completeness as a spec gate: three stories' principal defect landed in a combination the matrix did not enumerate, while every enumerated row was built correctly | Jamie | process | P6 |

Also recorded, not tracked: the eight section-F edge cases (deferred pending confirmation, each with a proposed guard), and the pre-existing deferred items this retro did not re-open — hardening headers and CSP, the unbounded re-walk per request against NFR-7, and `refs/snapshots/pending-cleanup` (a local-only safety ref from the session start, now redundant).

## Acceptance verdict

**Verdict: rejected.** Criteria: **declared**. Human decision: the machine verdict was accepted rather than overridden.

`pending_stories` is empty — all 13 stories are genuinely `done` — so the verdict does **not** rest on incomplete delivery. It rests on two independent grounds.

**1. A blocking finding stands unresolved (E1).** Not a test gap: reproduced end to end against the built CLI. A project whose root the tool cannot read is reported as `The scan finished. Artifacts examined: 0.` beside `A BMAD project, with no artifacts yet.` The pass recorded `complete: false` and `startEntry: unreadable`; the projection discarded both. The epic's headline deliverable misreports the one condition it most needs to be honest about.

**2. Five declared criteria are documented as unmet.** On `epics.md:275`'s own construction the declared set is the 24 FRs at `:253`, the NFR/UX-DR assignment at `:269`, and the 12 per-story done-conditions:

| Criterion | State | Source |
|---|---|---|
| FR-10 artifact roots from config | not implemented, yet claimed as covered in three places | D1 |
| FR-71's "check that matters" | recorded undeliverable and not delivered | `deferred-work.md:375` |
| NFR-14 keyboard operation | nothing on the surface is keyboard-operable; no keyboard or ARIA test exists anywhere | `deferred-work.md:523` |
| NFR-6 / AD-13 | no subject exists in the code | `deferred-work.md:339` |
| NFR-17 sanitizer | both named surfaces deferred; only `story_location` covered | `deferred-work.md:437` |

**What the verdict is not saying.** The epic delivered a working tool: 834 tests green with zero skips, and the behaviour check passed on every flow but the one in E1 — including the security-relevant ones (escaping, confinement, out-of-tree refusal, Host and method gates). Four of the five unmet criteria were identified and recorded *by the story that could not deliver them*, with triggers naming who picks them up. That is the discipline working. FR-10 is the exception, and it is a documentation contradiction rather than a surprise.

A re-render after action item 1 lands would likely reach **accepted-with-open-items**, since the remaining four unmet criteria are tracked deferrals rather than blockers.

## Open questions

Answers here would materially change the work:

1. **Is the ports ring intended?** The spine names hexagonal architecture in its frontmatter and declares `src/ports/` with three edges, none of which was built. Either the paradigm claim comes out of the document or the ring goes in. Nothing in the epic's evidence settles which — no story was ever assigned it (A1).
2. **Does FR-10 stay deferred?** Its cost is stated (`deferred-work.md:147`: artifact roots stay at documented defaults, "a project that puts them elsewhere is read at the wrong paths"). Every measured root table in the tool is pinned to BMAD v6.11.0, which makes NFR-5's "tolerates other BMAD versions" partly aspirational (D1, D6).
3. **Is `Not checked` on nearly every row acceptable on screen?** Story 1.9's accepted limit means 55 of 56 rows on this repository read `Not checked` for readability. `deferred-work.md:331` explicitly leaves this as "a decision about copy, not about this trade" for the first surface — which now exists.
4. **Who owns propagating a deferral decision?** FR-10's deferral was recorded in two documents and contradicted in four. Implementers correctly declined to edit binding documents; nothing else picked it up (D1, D8).
5. **Unresolved by the analyses:** whether the eight section-F edge cases reproduce as reported — they were not individually re-verified by the author — and whether NFR-12's Windows and case-insensitive-volume claims hold, since nothing in this epic has ever executed on either platform (`deferred-work.md:179`, `:191`).
