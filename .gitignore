# Dependencies
node_modules/

# Build output — regenerated by `npm run build`, and `prepack` rebuilds it before
# publish, so a committed copy could only ever be stale or conflicting.
dist/

# npm diagnostics
npm-debug.log*
*.tgz

# Editor and OS
.vscode/
.idea/
*.swp
.DS_Store
